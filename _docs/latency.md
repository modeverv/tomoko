# Latency Measurements

M1 Phase 0 creates this log before the first measured audio path exists.

| Date | Phase | Target | Result | Notes |
|---|---|---:|---:|---|
| 2026-05-23 | M1 Phase 0 | unit tests pass | pass | `pytest -m unit`: 3 passed in 0.01s. Runtime setup only; E2E measurement starts in Phase 1. |
| 2026-05-29 | Short working memory UI experiment | unit regression / static browser UI | pass | Short memory extraction is scheduled after `reply_done` and is not awaited before reply completion. Focused unit: `.venv/bin/python -m pytest -m unit tests/unit/test_short_memory.py tests/unit/test_phase88_context_snapshot.py::test_tomoro_session_passes_context_snapshot_to_thinking_input -q`: 9 passed in 0.25s. Full unit: 416 passed, 17 deselected in 0.97s. Static browser check on `http://127.0.0.1:8766/client/index.html` verified the Monitor panel layout; real mic first-audio latency still needs human runtime confirmation. |
| 2026-05-23 | M1 Phase 1 | local `/ws` float32 echo round trip | avg 0.13ms / p95 0.15ms | 100 chunks, 512 float32 samples per chunk, `uvicorn server.edge.main:app --host 127.0.0.1 --port 8000`. Browser mic/audio output latency still requires manual Chrome check. |
| 2026-05-23 | M1 Phase 2 | VAD silence threshold detection | 300ms -> 320ms / 400ms -> 416ms / 500ms -> 512ms | Synthetic scorer, 512 samples per chunk at 16kHz. Detection is quantized to 32ms chunks; real microphone + Silero model timing still requires manual Chrome check. |
| 2026-05-23 | M1 Phase 3 | WakeWordJudge + ambient_logs write | judge avg 0.0006ms / p95 0.0006ms; DB avg 4.74ms / p95 15.69ms | Wake word measured 1000 calls in-process. DB measured 20 async inserts to local PostgreSQL, then deleted marker rows. STT model latency still needs real utterance measurement with faster-whisper. |
| 2026-05-23 | M1 Phase 3 | no-echo WebSocket regression | pass | `pytest -m unit`: 14 passed in 0.11s. Echo playback removed after Phase 2 manual Chrome check; no audio round trip is measured in Phase 3 because mic input is no longer returned to the client. |
| 2026-05-23 | M1 Phase 4 | Ollama `qwen2.5:7b` first text delta | 17931.7ms | `ThinkFastMode` with `InferenceRouter` and local Ollama. Prompt: `トモコ、短く一言で返事して。`; measured first `reply_text` delta. This includes cold model load and is not acceptable for final 800ms E2E, but Phase 4 text streaming path is functional. |
| 2026-05-23 | M1 Phase 5 | VAD end -> first `say` audio chunk | 664.1ms | Synthetic VAD/STT/LLM path with real macOS `say -v Kyoko`; generated 14,886 bytes of AIFF for `うん。`. `pytest -m perf --tb=short`: 1 passed in 0.92s. Full real-mic E2E still depends on STT and Ollama warm state. |
| 2026-05-23 | M1 Phase 5 fix | VAD end -> first `say` WAV audio chunk | pass (<800ms) | `say` output changed from AIFF to 16kHz/16bit RIFF/WAVE for Chrome `decodeAudioData` compatibility. `pytest tests/perf/test_phase5_latency.py -m perf --tb=short`: 1 passed in 0.78s. Real `SayBackend` probe returned `RIFF...WAVE`, 31,064 bytes for `こんにちは。`. |
| 2026-05-23 | M1 Phase 6a | emotion line split + TTS path regression | pass (<800ms) | `EMOTION:<value>` is split before reply/TTS. Existing synthetic VAD/STT/LLM + real `say` perf still passes: `pytest tests/perf/test_phase5_latency.py -m perf --tb=short`: 1 passed in 0.79s. |
| 2026-05-23 | M1 Phase 6b | emotion image event + TTS style regression | pass (<800ms) | Emotion events now include an image path before TTS audio is emitted, and `say` maps all current emotions to rates. Existing synthetic VAD/STT/LLM + real `say` perf still passes: `pytest tests/perf/test_phase5_latency.py -m perf --tb=short`: 1 passed in 0.79s. |
| 2026-05-23 | Phase 6.5 | AttentionMode state-machine regression | pass (<800ms) | `TomoroSession` now emits attention events and records attended metadata. Existing synthetic VAD/STT/LLM + real `say` perf still passes: `pytest tests/perf/test_phase5_latency.py -m perf --tb=short -q`: 1 passed in 0.80s. |
| 2026-05-23 | Phase 6.6.0 | BargeInDetector unit regression | pass | AEC alone was not enough for MacBook speaker echo, so server-side TTS playback-window classification was added. `pytest -m unit`: 40 passed, 1 deselected in 0.24s. Barge-in focused tests: `pytest -m unit tests/unit/test_barge_in.py`: 5 passed in 0.13s. |
| 2026-05-23 | Phase 6.6.1 | AudioPlaybackControl regression | pass | Added `audio_start` / `audio_end` / `audio_control stop` with `turn_id` and client-side source stopping. `pytest -m unit`: 40 passed, 1 deselected in 0.25s. `node --check client/main.js`: pass. |
| 2026-06-13 | VOICEVOX chunk under-run mitigation | `voicevox_tsumugi_chunked` config | live recheck needed | Runtime logs showed 0.2s WAV chunks could arrive slower than playback, leaving playback telemetry gaps. Changed central / edge `segment_length` to 0.6 and `async-voicevox` launcher default `CPU_NUM_THREADS` to 4. Focused config/VOICEVOX unit validates the contract; live browser audio continuity still needs confirmation. |
| 2026-05-23 | STT backend benchmark | faster-whisper small vs MLX Whisper small | faster-whisper measured 977.5ms / MLX measured 102.1ms | `make bench-stt` with macOS `say` generated sample. faster-whisper `warm_ms=975.5`, `measured_ms=977.5`; MLX `warm_ms=13404.8` including first model fetch/cache, `measured_ms=102.1` after warm. Text: faster-whisper `ともこ、さんたすさんはいくつですか?`; MLX `ともこ 3たす3はいくつですか`. |
| 2026-05-23 | Startup STT warm-up | MLX Whisper startup warm-up | 2015.5ms | Direct `_warm_up_app()` run with cached `mlx-community/whisper-small-mlx`. Startup now runs configured STT backend warm-up before accepting WebSocket sessions; faster-whisper warm-up is a no-op. |
| 2026-05-23 | Kokoro MLX streaming TTS integration | synthetic VAD/STT/LLM + real `say` regression | pass (<800ms) | Reply/TTS generation is now background-task based so `/ws` can keep receiving mic chunks during TTS. Existing real `say` perf regression still passes: `pytest -m perf --tb=short tests/perf/test_phase5_latency.py`: 1 passed in 0.88s. Kokoro real model latency is not measured yet; unit coverage verifies `generate_stream()` chunk-to-WAV streaming and cancellation behavior. |
| 2026-05-24 | Irodori MLX TTS smoke | `mlx-community/Irodori-TTS-500M-v3-8bit` cached short synthesis | 2959.1ms | `mlx-audio` GitHub latest (`364585f`) with text `こんにちは。`, voice/ref_audio `none`, `num_steps=24`, sway sampling. Returned 1 RIFF/WAVE chunk, 126,764 bytes. Initial model download was ~1.09GB and is excluded from this cached smoke. |
| 2026-05-24 | Startup warm-up | MLX Whisper + Irodori MLX TTS warm-up | STT 1262.1ms / TTS 2831.9ms | Direct `_warm_up_app()` run with cached `mlx-community/whisper-small-mlx` and `mlx-community/Irodori-TTS-500M-v3-8bit`. Startup now warms and caches the default TTS backend before WebSocket sessions. |
| 2026-05-24 | Irodori MLX streaming backend smoke | warm-up済み `irodori_mlx_stream` first chunk / full text | first 107.0ms / total 206.9ms | Text `うん、わかった。少し待ってね。` was split into 2 Irodori v3 generations. Backend uses `seconds` to skip duration prediction and `num_steps=6` sway sampling. Cold warm-up in the same probe was 2679.4ms and remains paid at startup. |
| 2026-05-24 | TTS backend comparison | `irodori_mlx` / `irodori_mlx_stream` / Qwen3 small / Qwen3 large | first 659.2ms / 96.6ms / 142.6ms / 216.7ms | Cached run of `_tools/bench_tts_backends.py` with `うん、わかった。少し待ってね。`. Totals: 659.2ms / 192.7ms / 545.3ms / 820.5ms. Warm-up: 2933.0ms / 1310.8ms / 511.8ms / 544.2ms. WAVs are generated under `logs/tts-bench/` for local listening. |
| 2026-05-24 | Gemma 4 E2B TTS text normalizer | `mlx-community/gemma-4-e2b-it-4bit` via `mlx-vlm` | first token 87.4-184.1ms / first text 162.7-243.6ms / total 166.5-247.3ms | `_tools/bench_tts_text_normalizer_mlx.py`, cached model. Load was 3364.8ms. Mixed English/Japanese samples were converted to TTS-friendly Japanese; detailed outputs are in `logs/tts-text-normalizer/gemma4-e2b/summary.md`. |
| 2026-05-24 | TTS path Gemma normalizer smoke | `ReplySpeechNormalizer` via `mlx-vlm` | cold 3756.2ms / warm 163.2ms | Text `トモコ、today の meeting は 3pm からだよ。` -> `トモコ、今日の会議は午後三時からですよ。`. Startup warm-up now loads and runs the normalizer once, so the conversation path should see the warm number. Combined with cached `irodori_mlx_stream` x1 total ~192.7ms, mixed-text TTS is expected around 360ms plus queue overhead. |
| 2026-05-24 | Gemma normalizer + Irodori stream asuka x1.2 | `ReplySpeechNormalizer` + `irodori_mlx_stream` + `_tools/asset-factory_for_work/asuka.wav` | normalize 191.0ms / first 432.4ms / total 1198.2ms | Text `トモコ、today の meeting は 3pm からだから、schedule を確認して。終わったら quick に教えて。` -> `トモコ今日の会議は午後三時からだからスケジュールを確認して終わったらすぐに教えて`. WAV saved at `logs/tts-normalized-irodori-asuka-x1_2/gemma-normalized-irodori-stream-asuka-x1_2.wav`; audio duration 4.68s, 3 chunks. |
| 2026-05-24 | Gemma normalizer + Irodori stream asuka x1.5 | `ReplySpeechNormalizer` + `irodori_mlx_stream` + `_tools/asset-factory_for_work/asuka.wav` | normalize 191.7ms / first 463.0ms / total 1264.6ms | Text `トモコ、today の meeting は 3pm からだから、schedule を確認して。終わったら quick に教えて。` -> `トモコ今日の会議は午後三時からだからスケジュールを確認して終わったらすぐに教えて`. WAV saved at `logs/tts-normalized-irodori-asuka-x1_5/gemma-normalized-irodori-stream-asuka-x1_5.wav`; audio duration 5.88s, 3 chunks. |
| 2026-05-24 | Gemma normalizer + Kokoro MLX | `ReplySpeechNormalizer` + `KokoroMLXBackend` voice `jf_alpha` | normalize 188.6ms / raw 293.9ms / normalized 142.9ms | Same mixed text. Raw mixed Kokoro WAV: `logs/tts-normalized-kokoro-mlx/kokoro-mlx-raw-mixed.wav`, 13.25s, 1 chunk. Gemma normalized Kokoro WAV: `logs/tts-normalized-kokoro-mlx/kokoro-mlx-gemma-normalized.wav`, 7.175s, 1 chunk. |
| 2026-05-24 | Per-flush Gemma normalizer + Kokoro MLX | `ReplySpeechNormalizer` per sentence + `KokoroMLXBackend` voice `jf_alpha` | normalize 168.7ms + 103.4ms / first 100.0ms / TTS total 178.7ms | More realistic Tomoko path: first raw sentence `トモコ、today の meeting は 3pm からだから、schedule を確認して。` -> `トモコ今日の会議は午後三時からだからスケジュールを確認して`, second sentence -> `終わったらすぐに教えて`. WAV: `logs/tts-normalized-kokoro-mlx-analysis/kokoro-gemma-normalized-per-flush.wav`; audio duration 7.75s, 2 chunks. |
| 2026-05-24 | Adopted Kokoro + punctuation-preserving Gemma normalizer | `ReplySpeechNormalizer` per sentence + `KokoroMLXBackend` voice `jf_alpha` | normalize 211.1ms + 158.4ms / first 112.8ms / TTS total 166.6ms | Default TTS is now `kokoro_mlx`. Normalized parts: `トモコ、今日の会議は午後三時からだから、スケジュールを確認して。` / `終わったらすぐに教えて。`. WAV: `logs/tts-kokoro-gemma-adopted/kokoro-gemma-punctuation-preserved.wav`; audio duration 8.7s, 2 chunks. |
| 2026-05-24 | Bunsetsu flush Kokoro + merged short vocative | `ReplyAudioPlanner` soft comma flush + `ReplySpeechNormalizer` + `KokoroMLXBackend` | normalize 186.9ms + 142.1ms + 158.6ms / first 75.6ms / TTS total 191.6ms | `トモコ、` is no longer flushed alone; raw units are `トモコ、today の meeting は 3pm からだから、` / `schedule を確認して。` / `終わったら quick に教えて。`. WAV: `logs/tts-kokoro-gemma-bunsetsu-merged/kokoro-gemma-bunsetsu-merged.wav`; audio duration 9.8s, 3 chunks. |
| 2026-06-02 | Persistent task ledger context | active task DB slice / ContextSnapshotBuilder source | Postgres context build fast avg 0.541ms / max 4.881ms, deep avg 0.561ms / max 5.083ms | Added `task_ledger_entries`, `PostgresTaskLedgerStore`, and `task_ledger` context source. fast / normal reads top 10 active tasks, deep reads up to 25, reflective up to 50. Bench inserted 12 active tasks and ran 10 fast + 10 deep builds through Postgres store. Focused unit passed: 3 passed. DB integration passed: 1 passed. Full unit passed: 578 passed, 20 deselected. |
| 2026-06-02 | Task ledger voice create/complete | conversation hot path / background complete extraction | focused unit 28 passed in 0.31s; integration 1 passed in 0.19s | Rule-first create/complete detection runs in-process before participation. Ambiguous complete only schedules background `memory_extraction` structured extraction; main reply receives acknowledgement directive and does not await DB confirmation. update/cancel are unsupported and do not mutate rows. |

| 2026-05-24 | TTS-ready conversation prompt bench | `qwen2.5:7b` baseline vs TTS-ready prompt vs few-shot prompt | baseline 2/8 ready / TTS-ready 0/8 / few-shot 2/8 | `_tools/bench_tts_ready_prompt.py`. Prompt-only control did not reliably prevent English/copy-through output, so Gemma TTS normalization remains necessary as a fallback. Summaries: `logs/tts-ready-prompt-bench-examples/summary.md`. |
| 2026-05-24 | TTS-ready conversation prompt bench | `gemma-4-e2b-it-4bit` via `mlx-vlm`, startup-warmed | baseline 8/8 ready, avg first body 204.7ms / TTS-ready 7/8, 312.1ms / few-shot 8/8, 385.2ms | `_tools/bench_tts_ready_prompt.py --backend gemma_mlx`. Gemma follows Japanese/TTS constraints much better than `qwen2.5:7b`; baseline prompt was fastest and already avoided Gemma normalization on all 8 mixed-English samples. Summary: `logs/tts-ready-prompt-bench-gemma4-e2b-warm/summary.md`. |
| 2026-05-24 | Startup warm-up | MLX Whisper + Kokoro MLX + Gemma 4 E2B conversation backend | STT 1356.5ms / TTS 277.8ms / Gemma conversation 3751.6ms | `TOMOKO_LOG_FILE=logs/gemma-kokoro-warmup-smoke.log ... _warm_up_app()`. Default conversation backend is now `local_gemma4_e2b_mlx`; TTS normalizer is disabled, so startup skips `tts_text_normalizer`. Runtime latency logs now include `speech_end`, `first_reply_text`, `tts_start`, and `first_audio_chunk`. |
| 2026-05-24 | Startup warm-up | MLX Whisper + Kokoro MLX + LM Studio Gemma 4 E2B conversation backend | STT 1854.2ms / TTS 279.8ms / LM Studio conversation 243.0ms | `TOMOKO_LOG_FILE=logs/lmstudio-kokoro-warmup-smoke.log ... _warm_up_app()`. Default conversation backend is now `lmstudio_gemma4_e2b` via LM Studio OpenAI-compatible streaming API at `http://192.168.11.66:1234`; model `gemma-4-e2b-it-mlx`. After dependency sync, the same smoke still passed with LM Studio conversation 257.1ms. TTS normalizer remains disabled. |
| 2026-05-24 | M2 Phase 7 | short-term memory context regression | pass (<800ms) | `ThinkFastMode` now sends recent `conversation_logs` turns as chat history before the current user message. Synthetic VAD/STT/LLM + real `say` perf regression still passes: `pytest -m perf --tb=short tests/perf/test_phase5_latency.py`: 1 passed in 0.87s. `pytest -m unit`: 113 passed, 3 deselected in 0.33s. |
| 2026-05-24 | M2 Phase 7 | conversation log status migration regression | pass (<800ms) | Added `conversation_logs.status TEXT NOT NULL DEFAULT 'completed'` and applied the ALTER to local PostgreSQL. Hard-interrupted Tomoko replies are saved as `status='interrupted'`, while short-term context reads only `completed` rows. `pytest -m unit`: 113 passed, 3 deselected in 0.36s. `pytest -m perf --tb=short tests/perf/test_phase5_latency.py`: 1 passed in 0.87s. |
| 2026-05-24 | M2 Phase 8 | multilingual-e5-small embedding + pgvector search | warm 8085.2ms / query embed 33.0ms / search 14.4ms | Added `conversation_embeddings` with pgvector HNSW cosine index. Cached `intfloat/multilingual-e5-small` generated 384-dim query embeddings; local PostgreSQL search over 3 backfilled turns returned 3 hits. Backfill command: `mise exec -- uv run python _tools/embed_conversation_logs.py --limit 3`. |
| 2026-05-24 | M2 Phase 8 | long-term memory regression | pass (<800ms) | `ThinkDeepMode` injects top-K memory hits into the system prompt, and `TomoroSession` selects deep mode for memory cues / longer reflective utterances. `pytest -m unit`: 118 passed, 3 deselected in 0.36s. `pytest -m perf --tb=short tests/perf/test_phase5_latency.py`: 1 passed in 0.83s. |
| 2026-05-24 | M2 Phase 8.5 | conversation session boundary regression | pass (<800ms) | Added `conversation_sessions` and `conversation_logs.conversation_session_id`, applied DDL to local PostgreSQL, and made same-session context the first short-term context source. `pytest -m unit`: 123 passed, 3 deselected in 0.40s. `pytest -m perf --tb=short tests/perf/test_phase5_latency.py`: 1 passed in 0.89s. |
| 2026-05-24 | M2 Phase 8.6 | session summary index regression | pass (<800ms) | Added background `SessionSummarizer`, session summary vector search, and `conversation_sessions.summary_embedding` HNSW index. Online path still only closes sessions to `pending`; summary generation is background-only. `pytest -m unit`: 129 passed, 4 deselected in 0.38s. `pytest -m integration tests/integration/test_phase86_session_summary_db.py`: 1 passed in 0.19s. `pytest -m perf --tb=short tests/perf/test_phase5_latency.py`: 1 passed in 0.92s. |
| 2026-05-24 | M2 Phase 8.7 | persona JSONB snapshot regression | pass | Added `persona_lexicon_versions` / `persona_state_versions`, JSONB GIN indexes, model round-trip loaders, and background persona updater entrypoints. Online conversation path is unchanged. `pytest -m unit`: 136 passed, 5 deselected in 0.39s. `pytest -m integration tests/integration/test_phase87_persona_snapshots_db.py`: 1 passed in 0.11s. |
| 2026-05-24 | M2 Phase 8.8 | ContextSnapshotBuilder fast snapshot | pass (<20ms) | Added `ContextSnapshotBuilder`, context policy/trace/snapshot DTOs, and moved `TomoroSession` reply context loading through the builder. `pytest -m unit`: 141 passed, 6 deselected in 0.59s. `pytest -m perf --tb=short tests/perf/test_context_snapshot_latency.py`: 1 passed in 0.07s. |
| 2026-05-24 | M2 Phase 8.8.1 | ContextSnapshotBuilder cache hardening | pass (<20ms) | Added process-local TTL cache, source cache hit / age_ms / ttl_ms trace, and `ContextBuildPolicy.max_parallel_sources`. `pytest -m unit`: 144 passed, 6 deselected in 0.41s. `pytest -m perf --tb=short tests/perf/test_context_snapshot_latency.py`: 1 passed in 0.07s. |
| 2026-05-24 | M2 Phase 8.8.5 | TomoroSession event-shaped runtime regression | pass (<800ms) | Added runtime state/event/command DTOs, `get_now_state()`, `post_event()`, and playback telemetry event handling. `pytest -m unit`: 147 passed, 6 deselected in 0.43s. `pytest -m perf --tb=short tests/perf/test_phase5_latency.py`: 1 passed in 0.90s. |
| 2026-05-24 | M3 Phase 9.0 | candidate schema / DTO / store regression | pass | Added `utterance_candidates` / `arrival_candidates`, candidate DTOs, and `PostgresCandidateStore`. No online latency path was changed. `pytest -m unit`: 151 passed, 7 deselected in 0.38s. `pytest -m integration tests/integration/test_phase90_candidates_db.py`: 1 passed in 0.11s. |
| 2026-05-24 | M3 Phase 9.1 | deterministic source / selection regression | pass | Added `TimeBasedSource`, `CandidateSeed`, dedupe-by-context-tag, and `HighestPriority`. No online latency path was changed. Focused unit regression: `pytest -m unit tests/unit/test_phase90_candidates.py tests/unit/test_phase91_deterministic_sources.py`: 8 passed in 0.04s. |
| 2026-05-24 | M3 Phase 9.2 | LLM evaluator regression | pass | Added `LLMUtteranceEvaluator`, `candidate_gen` router role, and `maturity=1` save path. No online latency path was changed. Focused unit regression: `pytest -m unit tests/unit/test_router.py tests/unit/test_phase90_candidates.py tests/unit/test_phase91_deterministic_sources.py tests/unit/test_phase92_llm_evaluator.py`: 18 passed in 0.16s. |
| 2026-05-24 | M3 Phase 9.3 | arrival precompute fake backend | pass (<20ms) | Added `ArrivalPrecomputer`, Phase 9.3 `ArrivalContextSnapshot` schema, and `wait_silent` fallback. No online latency path was changed. Focused unit regression: `pytest -m unit tests/unit/test_phase90_candidates.py tests/unit/test_phase91_deterministic_sources.py tests/unit/test_phase92_llm_evaluator.py tests/unit/test_phase93_arrival_precompute.py`: 18 passed in 0.14s. Perf: `pytest -m perf --tb=short tests/perf/test_phase93_arrival_precompute_latency.py`: 1 passed in 0.11s. |
| 2026-05-24 | M3 Phase 9.4 | thinker-once background smoke | candidate 931.8ms / arrival 657.1ms | `make thinker-once` with LM Studio candidate_gen at `http://192.168.11.66:1234`: generated_seed_count=1, inserted_seed_count=1, kept_candidate_count=1, arrival_behavior=wait_silent. No online `/ws` latency path was changed. Focused unit: `pytest -m unit tests/unit/test_phase94_thinker_loop.py`: 3 passed in 0.13s. Integration smoke: `pytest -m integration tests/integration/test_phase94_thinker_smoke.py`: 1 passed in 0.21s. |
| 2026-05-24 | M3 Phase 10 / Phase 11.3 partial | candidate consumption unit regression | pass | Added online candidate command runner, `session_started` arrival fetch, 45s idle initiative tick, and cached audio consumption for pregenerated candidates. Real browser first-audio latency is not measured in this unit-only pass. Focused unit regression: `pytest -m unit tests/unit/test_phase10_session_contract.py tests/unit/test_phase10_candidate_command_runner.py tests/unit/test_phase1_echo.py tests/unit/test_phase885_session_runtime.py`: 17 passed in 0.27s. |
| 2026-05-24 | M3 Phase 11.0 / 11.3 / 11.4 | pregenerated audio contract regression | pass | Fixed reply ordering to `reply_text` / `audio_start` / binary / `audio_end` / `reply_done`, enforced `maturity=2` as text+audio, and added `pregenerated_audio_chunks` store/table for future multi-chunk pregeneration. No browser latency was measured in this pass. Focused unit: `pytest -m unit tests/unit/test_phase110_pregenerated_candidate.py tests/unit/test_phase10_candidate_command_runner.py`: 9 passed in 0.18s. Dedicated Phase 11.3 unit: `pytest -m unit tests/unit/test_phase113_pregenerated_audio_consumption.py`: 1 passed in 0.16s. Integration: `pytest -m integration tests/integration/test_phase90_candidates_db.py`: 1 passed in 0.14s. Full unit: `pytest -m unit`: 194 passed, 9 deselected in 0.48s. |
| 2026-05-24 | M3 Phase 11.1 / 11.2 | pregenerator unit regression | pass | Added `UtterancePregenerator` and wired optional pregeneration into `ThinkerProcess.run_once()` / candidate loop. Real TTS latency is not measured in this fake-backend pass. Focused unit regression: `pytest -m unit tests/unit/test_phase94_thinker_loop.py tests/unit/test_phase111_pregenerator.py`: 6 passed in 0.16s. |
| 2026-05-24 | M3 Phase 12.0 | diary store unit regression | pass | Added `diary_entries` DDL and `DiaryEntry` / `DiaryStore` / `PostgresDiaryStore`. No online latency path was changed. Focused unit regression: `pytest -m unit tests/unit/test_phase120_diary_store.py`: 3 passed in 0.04s. |
| 2026-05-24 | M3 Phase 12.1-12.4 | journalist background regression | pass | Added `JournalistInputBuilder`, `DiaryWriter`, `DiarySource`, `run_journalist.py`, Makefile targets, and diary role routing. No online `/ws` latency path was changed. Focused unit: `pytest -m unit tests/unit/test_phase120_diary_store.py tests/unit/test_phase121_journalist_input.py tests/unit/test_phase122_journalist_writer.py tests/unit/test_phase123_diary_source.py tests/unit/test_phase124_journalist_process.py tests/unit/test_router.py`: 17 passed in 0.19s. Integration: `pytest -m integration tests/integration/test_phase120_diary_db.py`: 1 passed in 0.07s. Full unit: `pytest -m unit`: 203 passed, 10 deselected in 0.50s. |
| 2026-05-24 | M4 Phase 14.0-14.4 partial | edge split / duplicate filtering regression | pass | Added presence/edge status schema, presence DTO/store, `DirectSpeakerResolver`, `DuplicateSpeechFilter`, `PresenceManager`, `config/edge_kitchen.toml`, and local `make edge-kitchen` / `make gateway` targets. No online `/ws` latency path was changed; this pass fixes edge/gateway boundary contracts and duplicate detection only. Focused unit: `pytest -m unit tests/unit/test_phase14_edge_split.py`: 6 passed in 0.08s. Integration: `pytest -m integration tests/integration/test_phase14_presence_db.py`: 1 passed in 0.10s. |
| 2026-05-25 | M4 Phase 14.3 | edge / gateway WebSocket protocol regression | pass | Added `/edge/ws`, edge JSON protocol DTOs, remote edge speech adapter, gateway protocol handler, and edge-local TTS reply playback. Central `/ws` browser client remains available. No real LAN latency measured yet. `ruff check .`: pass. `pytest -m unit`: 214 passed, 11 deselected in 0.88s. `pytest -m integration tests/integration/test_phase14_presence_db.py`: 1 passed in 0.11s. |
| 2026-05-24 | M4 Phase 13.0 / 13.1 | inference monitor unit regression | pass | Added `inference_metrics`, `InferenceMetricSample`, metric stores, and `BackendHealthMonitor`. Router still reads latest metrics only and does not probe in online `select()`. Focused unit regression: `pytest -m unit tests/unit/test_phase13_inference_monitor.py tests/unit/test_router.py`: 8 passed in 0.16s. |
| 2026-05-24 | Session 53 full unit regression | all unit tests | pass | `mise exec -- uv run ruff check .`: pass. `mise exec -- uv run pytest -m unit`: 188 passed, 9 deselected in 0.49s. |
| 2026-05-24 | Phase 10 human decision follow-up | all unit tests | pass | Initiative / arrival speech no longer starts a conversation session by itself; a human reply starts the session through the normal participation path. `pytest -m unit tests/unit/test_phase10_candidate_command_runner.py tests/unit/test_phase10_session_contract.py`: 12 passed in 0.18s. `pytest -m unit`: 189 passed, 9 deselected in 0.46s. |
| 2026-05-25 | M3 Phase 10.5 | TomoroSession runtime hardening regression | pass | Added internal `post_event()` queue/drain and candidate `request_id` stale-result suppression. No real browser latency was measured; this pass changes event ordering, not audio generation. Focused unit: `pytest -m unit tests/unit/test_phase105_session_runtime.py`: 5 passed in 0.31s. Related unit: `pytest -m unit tests/unit/test_phase885_session_runtime.py tests/unit/test_phase10_session_contract.py tests/unit/test_phase10_candidate_command_runner.py tests/unit/test_phase105_session_runtime.py`: 19 passed in 0.34s. Full unit: `pytest -m unit`: 222 passed, 11 deselected in 1.11s. |
| 2026-05-25 | LFM MLX backend addition | `mlx_lm` backend unit regression | pass | Added `MLXLMBackend` for `lfm2.5-1.2b-jp-mlx` without changing the active `conversation_backend`. Real model warm-up / first-token latency was not measured in this pass to avoid disturbing concurrent Phase 10.5 work. Focused unit: `pytest -m unit tests/unit/test_mlx_lm_backend.py tests/unit/test_router.py`: 10 passed in 0.30s. Full unit: `pytest -m unit`: 226 passed, 11 deselected in 1.00s. `ruff check .`: pass. |
| 2026-05-25 | LFM MLX conversation backend smoke | `LiquidAI/LFM2.5-1.2B-JP-MLX-bf16` via `mlx_lm` | cold first 4435.9ms / warm first 26.6ms | The initial short name `lfm2.5-1.2b-jp-mlx` was not a valid HF repo id, so config now uses the official MLX repo. Cached-file cold load + generate: first delta 4435.9ms, total 4485.4ms. Warm runs: first delta 26.6ms / 26.6ms, total 76.5ms / 75.4ms, warm averages first 26.6ms, total 76.0ms. Output: `うん、聞こえるよ。`. JSON: `logs/lfm25-mlx-latency-smoke.json`. Startup warm-up with configured STT/TTS/conversation/embedding: STT 2318.7ms, Kokoro 662.8ms, LFM conversation 3398.9ms, embedding 7291.9ms, total 13673.4ms. Log: `logs/lfm25-startup-warmup-smoke.log`. Active `conversation_backend` is now `local_lfm25_12b_jp_mlx`. `ruff check .`: pass. `pytest -m unit`: 230 passed, 11 deselected in 1.15s. |
| 2026-05-25 | LFM MLX 4bit conversation backend smoke | `lmstudio-community/LFM2.5-1.2B-Instruct-MLX-4bit` via `mlx_lm` | cold first 19022.9ms / warm first 20.6ms | User requested the LM Studio community 4-bit MLX model. Cached-file cold load + generate: first delta 19022.9ms, total 19041.4ms. Warm runs: first delta 20.8ms / 20.4ms, total 39.1ms / 38.8ms, warm averages first 20.6ms, total 39.0ms. Output: `はい、聞こえます。ありがとう。`. JSON: `logs/lfm25-4bit-mlx-latency-smoke.json`. Startup warm-up with configured STT/TTS/conversation/embedding: STT 1936.1ms, Kokoro 321.6ms, LFM 4bit conversation 3478.3ms, embedding 7085.3ms, total 12822.4ms. Log: `logs/lfm25-4bit-startup-warmup-smoke.log`. Active `conversation_backend` remains `local_lfm25_12b_jp_mlx`. `ruff check .`: pass. `pytest -m unit`: 230 passed, 11 deselected in 1.32s. |
| 2026-05-25 | M3 Phase 10.5 | start reason / priority policy regression | pass | Added `TomoroRuntimeState.last_start_reason`, normalized human start reasons to `wake_word` / `followup`, and added `start_reason` to initiative / arrival command payloads. No real browser latency was measured. Focused unit: `pytest -m unit tests/unit/test_phase105_session_runtime.py tests/unit/test_phase10_session_contract.py tests/unit/test_phase85_conversation_sessions.py tests/unit/test_phase885_session_runtime.py`: 24 passed in 0.37s. Full unit: `pytest -m unit`: 230 passed, 11 deselected in 1.14s. |
| 2026-05-25 | Whisper MLX vs CoreML benchmark | `mlx_whisper` vs `whisperkit-cli` CoreML | MLX measured 103.0ms / CoreML measured 4755.6ms | `TOMOKO_STT_BENCH_BACKENDS=local_whisper_mlx_small,local_whisperkit_coreml_small pytest -m perf --tb=short tests/perf/test_stt_latency.py -s`. MLX warm 1353.3ms, measured 103.0ms. WhisperKit CLI CoreML warm 35722.1ms, measured 4755.6ms. Both produced `ともこ 3たす3はいくつですか`. Current CoreML backend launches CLI per transcription, so this measures process/model startup overhead too; a practical CoreML STT path needs WhisperKit `serve` or another persistent process. |
| 2026-05-25 | Kokoro MLX vs CoreML benchmark | `KokoroMLXBackend` vs `kokoro say` CoreML | MLX first 87.9ms / CoreML first 4816.4ms | `_tools/bench_tts_backends.py --targets kokoro_mlx,kokoro_coreml --text "うん、わかった。少し待ってね。"`. MLX warm 0.0ms, total 88.0ms, 1 chunk, 2875.0ms audio. CoreML warm 8660.9ms, total 4816.5ms, 1 chunk, 2375.0ms audio. WAV samples: `logs/kokoro-mlx-coreml-bench/kokoro_mlx.wav` and `logs/kokoro-mlx-coreml-bench/kokoro_coreml.wav`. Japanese Kokoro CoreML requires IPA conversion via `misaki[ja]`; the CLI currently rejects `--stream` with `--ipa`, so Japanese CoreML falls back to file generation. |
| 2026-05-25 | WhisperKit serve backend benchmark | `mlx_whisper` vs persistent `whisperkit-cli serve` | MLX measured 103.8ms / WhisperKit serve measured 214.3ms | Added `WhisperKitServeSTT`, which calls `/v1/audio/transcriptions` on a persistent local WhisperKit server and starts `whisperkit-cli serve` if `/health` is unavailable. `TOMOKO_STT_BENCH_BACKENDS=local_whisper_mlx_small,local_whisperkit_serve_small pytest -m perf --tb=short tests/perf/test_stt_latency.py -s`: MLX warm 1111.8ms / measured 103.8ms; WhisperKit serve auto-start warm 4791.6ms / measured 214.3ms. Both produced `ともこ 3たす3はいくつですか`. After pytest exit, the auto-started server was terminated and port 50060 was clear. |
| 2026-05-25 | STT benchmark CLI | `mlx_whisper` vs persistent `whisperkit-cli serve` | MLX avg 103.3ms / WhisperKit serve avg 217.7ms | `_tools/bench_stt_backends.py --backends local_whisper_mlx_small,local_whisperkit_serve_small --runs 3 --output logs/stt-mlx-coreml-bench.json`. MLX warm 1111.2ms, min 102.4ms, max 104.9ms. WhisperKit serve auto-start warm 4787.2ms, min 213.3ms, max 220.2ms. Both produced `ともこ 3たす3はいくつですか`; after script exit, port 50060 was clear. |
| 2026-05-25 | STT concurrent workload benchmark | STT while `kokoro_mlx` TTS runs | MLX avg 203.5ms / WhisperKit serve avg 215.9ms | `_tools/bench_stt_backends.py --backends local_whisper_mlx_small,local_whisperkit_serve_small --runs 3 --load-tts-backend kokoro_mlx --load-tts-text "うん、わかった。少し待ってね。今日は少し長めに話します。" --output logs/stt-mlx-coreml-concurrent-tts-bench.json`. MLX min 154.9ms / max 300.5ms, load avg 201.5ms. WhisperKit serve min 214.6ms / max 218.1ms, load avg 117.6ms. TTS MLX concurrent load narrows the single-backend gap substantially. |
| 2026-05-25 | STT concurrent workload benchmark | STT while LFM MLX conversation runs | MLX avg 112.8ms / WhisperKit serve avg 205.4ms | `_tools/bench_stt_backends.py --backends local_whisper_mlx_small,local_whisperkit_serve_small --runs 3 --load-conversation-backend local_lfm25_12b_jp_mlx --load-conversation-text "トモコ、短く二文で返事して。" --output logs/stt-mlx-coreml-concurrent-llm-bench.json`. MLX min 111.2ms / max 113.7ms, load avg 52.2ms. WhisperKit serve min 203.2ms / max 207.1ms, load avg 59.2ms. Short LFM generation did not create enough overlapping MLX load to erase the MLX STT advantage. |
| 2026-05-25 | STT concurrent workload benchmark | STT while `kokoro_mlx` + LFM MLX run | MLX avg 161.4ms / WhisperKit serve avg 212.1ms | `_tools/bench_stt_backends.py --backends local_whisper_mlx_small,local_whisperkit_serve_small --runs 3 --load-tts-backend kokoro_mlx --load-conversation-backend local_lfm25_12b_jp_mlx --output logs/stt-mlx-coreml-concurrent-tts-llm-bench.json`. MLX min 160.0ms / max 162.7ms, load avg 190.6ms. WhisperKit serve min 208.7ms / max 214.4ms, load avg 166.0ms. After all runs, port 50060 was clear. |
| 2026-05-25 | STT 30-run tail benchmark | idle / `kokoro_mlx` / `kokoro_mlx` + LFM MLX | MLX p95 106.3 / 165.2 / 165.7ms; CoreML p95 222.0 / 216.9 / 216.0ms | 30 runs each with `_tools/bench_stt_backends.py`. JSON: `logs/stt-mlx-coreml-runs30-idle.json`, `logs/stt-mlx-coreml-runs30-tts.json`, `logs/stt-mlx-coreml-runs30-tts-llm.json`. Idle avg: MLX 103.8ms / CoreML 211.0ms. TTS load avg: MLX 157.0ms / CoreML 212.0ms. TTS+LLM load avg: MLX 159.3ms / CoreML 212.4ms. MLX remains faster even under these measured loads, while CoreML stays a stable fixed ~212ms lane. |
| 2026-05-25 | Supertonic-3 CoreML TTS smoke | Japanese `ja` / voice style `M1` / CPU_AND_NE | load 5533.7ms / synth avg 102.4ms | `_tools/bench_supertonic_coreml_tts.py --runs 5 --output-dir logs/supertonic-coreml-smoke --model-dir logs/supertonic-coreml-smoke/model` with `uv run --with coremltools`. Text: `こんにちは、トモコです。今日は少しだけ話してみます。`. Generated 4345.2ms audio, min 98.9ms / max 112.8ms, RTFx ~38.5-43.9x. WAVs and `summary.json` are under `logs/supertonic-coreml-smoke/`. HF cache symlinks had to be copied as real files before CoreML prediction; the tool handles this. |
| 2026-05-25 | Supertonic-3 CoreML female voice smoke | Japanese `ja` / voice styles `F1`-`F5` / CPU_AND_NE | F1 112.6ms / F2 111.3ms / F3 148.9ms / F4 108.1ms / F5 113.9ms | Female style JSONs came from `Reza2kn/supertonic-3-coreml` and were used with the existing `FluidInference/supertonic-3-coreml` CoreML packages. Text: `こんにちは、トモコです。今日は少しだけ話してみます。`. WAVs: `logs/supertonic-coreml-smoke/female/F1/ja-F1-run1.wav` through `F5/ja-F5-run1.wav`. Generated audio lengths were 3895.1-4471.7ms and RTFx 30.0-38.6x. |
| 2026-05-25 | BGE-M3 embedding adoption | `BAAI/bge-m3` via sentence-transformers | first cached-process embed 7838.9ms / warm 32.8ms | Switched embedding backend from `intfloat/multilingual-e5-small` 384-dim to `BAAI/bge-m3` 1024-dim. Initial HF download + first embed was 36990.5ms. After cache, a fresh process load + first embed was 7838.9ms and same-process warm query was 32.8ms. Applied `docker/postgres/init/006_bge_m3_embeddings.sql`, deleted 3 old e5 embeddings, and backfilled 20 turns as `BAAI/bge-m3` with `vector_dims=1024`. |
| 2026-05-25 | Supertonic-3 CoreML backend adoption | `supertonic_coreml_f1` / Japanese F1 / CPU_AND_NE | startup TTS warm-up 7963.4ms / warm synth 104.7ms | Added `SupertonicCoreMLBackend` and switched default `tts_backend` to `supertonic_coreml_f1`. `_tools/bench_tts_backends.py --targets supertonic_coreml_f1` generated `logs/tts-supertonic-coreml-backend/supertonic_coreml_f1.wav`; warm-up 9210.3ms, first/total 104.7ms, one 4345.2ms WAV chunk. FastAPI startup warm-up log: STT 1552.4ms, Supertonic TTS 7963.4ms, LFM conversation 1088.2ms, BGE-M3 embedding 6607.1ms. |
| 2026-05-25 | STT soak benchmark smoke | `local_whisper_mlx_small` continuous loop until Ctrl-C | avg 101.4ms / recent p95 104.7ms | `_tools/soak_stt_backends.py --backends local_whisper_mlx_small --status-interval-sec 2 --output logs/stt-soak-smoke.jsonl` ran 125 samples over 12.7s, qps 9.86, errors 0. The script now writes JSONL sample/error/summary records and supports TTS / conversation load backends for long-running contention tests. |
| 2026-05-25 | Voice stack scenario smoke | MLX STT vs WhisperKit serve while Supertonic CoreML TTS + LFM MLX run | MLX STT 109.6ms / CoreML STT 212.8ms | `_tools/soak_voice_stack_scenarios.py --max-cycles 1 --status-interval-sec 2 --output logs/voice-stack-soak-smoke.jsonl`. Default scenarios compare `local_whisper_mlx_small` and `local_whisperkit_serve_small` under the same `supertonic_coreml_f1` + `local_lfm25_12b_jp_mlx` load. In this one-cycle smoke, load elapsed was 95.7ms / 89.9ms and both STT outputs matched. |
| 2026-05-25 | Voice stack stress smoke | MLX STT vs WhisperKit serve under repeated Supertonic + LFM load | MLX STT 305.3ms / CoreML STT 429.3ms | `_tools/soak_voice_stack_scenarios.py --max-cycles 1 --status-interval-sec 2 --output logs/voice-stack-soak-stress-smoke.jsonl` after changing the default load to Supertonic TTS x2 plus LFM conversation x6 per measured STT call. Load elapsed was 1286.6ms / 1258.1ms, making the stress path substantially heavier than the earlier ~90ms load. |
| 2026-05-25 | M3 Phase 10.6 | TomokoDesire / Speakability policy regression | pass | Added deterministic candidate speak policy before initiative reply start. No real browser first-audio measurement was needed because audio generation path is unchanged; perf guard passed: `pytest -m perf --tb=short`: 7 passed, 1 skipped, 275 deselected in 24.19s. Full unit passed: `pytest -m unit`: 269 passed, 14 deselected in 0.70s. |
| 2026-05-25 | M3 Phase 10.6 completion | scoped feedback + LLM judge runner regression | pass | Added `initiative_feedback_signals`, scoped feedback summary, and `InitiativeLLMJudge`. Audio generation path is unchanged. Full unit: `pytest -m unit`: 273 passed, 15 deselected in 0.69s. Integration: `pytest -m integration tests/integration/test_phase106_initiative_feedback_db.py tests/integration/test_phase90_candidates_db.py`: 2 passed in 0.21s. Perf guard: `pytest -m perf --tb=short`: 7 passed, 1 skipped, 280 deselected in 23.98s. |
| 2026-05-25 | M3 Phase 10.9 | stop-intent queue + fixed WAV control response regression | pass | Added PostgreSQL stop-intent observation/signal queue, background classifier worker, stale-safe `stop_intent_classified` SessionEvent, and fixed `assets/audio/stop_ack.wav`. WAV generation: `say -v Kyoko --data-format=LEI16@16000 -o assets/audio/stop_ack.wav 'はい、止めます'`. Focused unit: `pytest -m unit tests/unit/test_stop_intent_queue.py tests/unit/test_stop_ack_audio.py tests/unit/test_phase105_session_runtime.py`: 18 passed in 0.35s. Integration: `pytest -m integration tests/integration/test_stop_intent_db.py`: 1 passed in 0.21s. Full unit: `pytest -m unit`: 285 passed, 16 deselected in 0.78s. |
| 2026-05-25 | M3 Phase 10.9 stop ACK voice correction | `supertonic_coreml_f1` fixed WAV | warmup 8867.0ms / first 2063.5ms / total 2063.5ms | Replaced the earlier Kyoko-generated `assets/audio/stop_ack.wav` with Supertonic-3 CoreML F1 so the control response matches Tomoko's default voice. Command: `_tools/bench_tts_backends.py --targets supertonic_coreml_f1 --text 'はい、止めます' --output-dir logs/stop-ack-supertonic-f1`, then copied `logs/stop-ack-supertonic-f1/supertonic_coreml_f1.wav` into `assets/audio/stop_ack.wav`. Output is RIFF/WAVE PCM 16-bit mono 44.1kHz, 138,430 bytes, 1569.0ms audio. |
| 2026-05-25 | M3 Phase 10.9 stop ACK clarity fix | Kyoko fixed WAV + 300ms tail silence | STT avg 89.2ms / text `はい、止めます` | The Supertonic F1 stop ACK sounded like it dropped the final `す`; `local_whisper_mlx_small` transcribed the previous asset as `四四四`, and Supertonic punctuation / long-vowel / F2-F5 variants were unstable. Regenerated with `say -v Kyoko --data-format=LEI16@16000 -o logs/stop-ack-kyoko-clear/stop_ack_raw.wav 'はい、止めます。'` and `sox ... assets/audio/stop_ack.wav pad 0 0.30`. Result: RIFF/WAVE PCM 16-bit mono 16kHz, 1414.9ms, tail 300ms silence. `bench_stt_backends.py --backends local_whisper_mlx_small --runs 3 --audio-file assets/audio/stop_ack.wav` returned `はい、止めます`. |
| 2026-05-25 | M3 Phase 10.9 stop ACK selected asset | Supertonic F1 `はい、止めますね。` | 1756.8ms / 154,996 bytes | Human listening selected `logs/stop-ack-supertonic-retry/phrase_tomemasu_ne.wav` over the Kyoko fallback. Copied it to `assets/audio/stop_ack.wav` and set `StopAckAudioProvider.text` to `はい、止めますね`. Output is RIFF/WAVE PCM 16-bit mono 44.1kHz. MLX Whisper remained unreliable on these very short Supertonic control phrases, so selection is based on human listening rather than STT text. |
| 2026-05-25 | Phase 18 external observation pipeline regression | raw Markdown / DB store / thinker source / journalist source | pass | External observation ingest / normalize / interpret is background-only and does not add online ContextSnapshotBuilder work. Validation: sample validator passed, `make information-ingest-dry-run` returned processed=0 with no DB/file changes, `ruff check .` passed, `pytest -m unit` passed 293 selected tests, `pytest -m integration` passed 9 selected tests, and `pytest -m perf --tb=short` passed 7 with 1 skipped in 23.96s. |
| 2026-05-25 | Turn-taking / STT backend adjustment | VAD 1000ms + stale unstarted reply cancel + WhisperKit serve STT | WhisperKit warm 7003.5ms / measured 211.9ms | `config/central_realtime.toml` now uses `vad_silence_ms = 1000` and `stt_backend = "local_whisperkit_serve_small"`. `TomoroSession` cancels a reply as stale if new `listening` arrives before any reply text/emotion/audio is emitted. Focused STT perf: `TOMOKO_STT_BENCH_BACKENDS=local_whisperkit_serve_small mise exec -- uv run pytest -m perf --tb=short tests/perf/test_stt_latency.py -s`: 1 passed, text `ともこ 3たす3はいくつですか`. Full unit: `pytest -m unit`: 297 passed, 17 deselected. |
| 2026-05-25 | Turn-taking browser playback adjustment | VAD 800ms + `audio_start` playback queue reset | unit/js syntax pass | `vad_silence_ms` was returned from 1000ms to 800ms for live feel comparison. Client `audio_start` now stops stale playback sources and resets `nextPlaybackTime` when the turn id changes, so an old queued turn should not delay the next turn's playback. Verification: `node --check client/main.js`, focused unit 7 passed, full unit 297 passed / 17 deselected. Real browser latency should be compared with `make server-debug`. |
| 2026-05-25 | VOICEVOX Tsumugi backend smoke | `voicevox.app` / speaker id 8 / CPU-side external Engine | first 364.7ms / total 364.9ms | Added `VoicevoxBackend` and switched central / edge `tts_backend` to `voicevox_tsumugi`. Smoke against `http://127.0.0.1:50021` with text `うん、わかった。少し待ってね。` returned one RIFF/WAVE chunk, 136,236 bytes, 24kHz mono, audio length 2837.3ms. WAV saved at `logs/voicevox-tsumugi-smoke.wav`. |
| 2026-05-25 | VOICEVOX stream backend smoke | `voicevox_tsumugi_stream` / `/cancellable_synthesis` with 404 fallback | first 347.5ms / total 347.6ms | VOICEVOX Engine 0.25.2 exposes `/cancellable_synthesis` in OpenAPI, but the running `voicevox.app` returned 404 for a real AudioQuery because experimental features are disabled by default. Added `VoicevoxStreamBackend`, which tries `/cancellable_synthesis` first and falls back to `/synthesis` while preserving one complete WAV per websocket audio chunk for the current browser decoder. WAV saved at `logs/voicevox-tsumugi-stream-smoke.wav`. |
| 2026-05-25 | STT accuracy / conversation prompt adjustment | WhisperKit serve large + voice conversation persona rules | unit pass, real latency not measured | Active STT backend changed to `local_whisperkit_serve_large` with model `large-v3-v20240930_626MB` on `127.0.0.1:50061`, keeping small serve on `50060` to avoid model/port ambiguity. `base_persona.md` now tells Tomoko to ask for confirmation when STT input is ambiguous, respond as in-development Tomoko for model/latency/debug talk, and avoid ending with empty backchannels. Verification: focused unit 24 passed, full unit 298 passed / 17 deselected. First large warm-up and transcript quality should be measured with `make server-debug`. |
| 2026-05-25 | LM Studio Gemma 4 E4B adoption | `gemma-4-e4b-it-mlx` via LM Studio OpenAI compatible API | first 313.5ms / total 314.6ms | Switched active `conversation_backend` to `lmstudio_gemma4_e4b` at `http://192.168.11.66:1234`, with `local_gemma4_e2b_mlx` as local Gemma fallback. Smoke prompt: `トモコ、短く一言で返事して。`; output: `はい。`. Focused unit: `pytest -m unit tests/unit/test_phase0_config.py tests/unit/test_router.py tests/unit/test_lm_studio_backend.py`: 16 passed. Full unit: `pytest -m unit`: 326 passed, 17 deselected. `ruff check .`: pass. |
| 2026-05-26 | WhisperKit large turbo 632MB CPU+ANE STT lane | `local_whisperkit_serve_large_turbo_632m_cpu_ne` | unit pass, real latency not measured | Added explicit `cpuAndNeuralEngine` compute-unit wiring for `WhisperKitServeSTT` and switched central STT to `large-v3-v20240930_turbo_632MB` on `127.0.0.1:50062`. This matches the image's `openai_whisper-large-v3-v20240930_turbo_632MB + .cpuAndNeuralEngine` lane more closely than the previous `large-v3-v20240930_626MB` test. Focused unit: `.venv/bin/python -m pytest -m unit tests/unit/test_stt_backends.py tests/unit/test_phase0_config.py`: 16 passed. Real browser transcript, ANE/GPU usage, and warm latency still need `make server-debug` measurement. |
| 2026-05-27 | LM Studio Gemma 4 26B A4B adoption for live trial | `gemma-4-26b-a4b-it-mlx` via LM Studio OpenAI compatible API | synthetic: first 0.31-0.41s after load / initial switch about 10s | Switched active `conversation_backend` to `lmstudio_gemma4_26b_a4b` for tomorrow's real browser trial, keeping `lmstudio_gemma4_e4b` as a quick rollback/comparison backend and `local_gemma4_e2b_mlx` as fallback. Synthetic multi-turn state simulation showed better semantic quality than E4B. Real voice latency and perceived conversation quality still need `make server-debug` measurement. |
| 2026-05-27 | Apple Speech STT vs MLX Whisper large turbo q4 | Apple Speech framework sidecar / `local_whisper_mlx_large_turbo_q4` | synthetic avg 183.3ms / 240.6ms; real read-aloud avg 242.7ms / 248.7ms | `_tools/bench_stt_backends.py --backends local_apple_speech_ja,local_whisper_mlx_large_turbo_q4 --runs 3`. Synthetic `say` text: Apple `智子3 +3 =いくつですか`, MLX `ともこ3たす3はいくつですか`. Real audio `work/audio-recordings/20260525T125851Z-read_aloud.wav`: Apple `うんそうだよ智子短いです声を捨てずに続き`, MLX `うんそうだよ ともこ 短い声を捨てずに 続きの`. JSON: `logs/stt-apple-speech-vs-mlx-large-turbo-q4.json` and `logs/stt-apple-speech-vs-mlx-large-turbo-q4-read-aloud.json`. |
| 2026-05-27 | Apple Speech active STT smoke | `local_apple_speech_ja` as active central realtime STT | warm 189.6ms / measured 196.3ms | `_tools/bench_stt_backends.py --backends local_apple_speech_ja --runs 1 --output logs/stt-apple-active-smoke.json`. Text: `智子3 +3 =いくつですか`. This run verifies the Apple Speech sidecar is executable before mactop observation. |
| 2026-05-29 | Short memory LLM extraction lane regression | `memory_extraction` role + LM Studio E2B structured output fallback | unit pass, real latency not measured | Added optional `memory_extraction_backend = "lmstudio_gemma4_e2b"` for post-reply short memory extraction. Runtime logs now include backend/source/decision/proposals/elapsed_ms and backend trace role `memory_extraction`. LM Studio queue key is model-specific so `gemma-4-26b-a4b-it-mlx` conversation and `gemma-4-e2b-it-mlx` extraction can be measured as separate model lanes. Real LM Studio parallelism and first-reply impact still need `make server-debug` measurement with both models loaded. |
| 2026-05-30 | Conversation prompt timestamp + prompt-only file log | prompt assembly/file append microbench | append avg 0.045ms | Added current local datetime/weekday to `ThinkFastMode` system prompt and appended the exact conversation LLM prompt payload to `logs/conversation-prompts.jsonl`. Microbench used 100 append calls against a temporary JSONL file: total 4.493ms / avg 0.045ms. Full unit passed: `.venv/bin/python -m pytest -m unit` -> 428 passed, 17 deselected. |
| 2026-05-30 | Persona overlay prompt injection | `ThinkFastMode._build_system_prompt()` with `prompts/persona_overlay.md` loaded at init | avg 0.0030ms | Added optional sibling `persona_overlay.md` prompt block. Overlay file is read once during `ThinkFastMode` init; prompt build microbench used 1000 builds after warm-up: total 2.978ms / avg 0.0030ms. Capture backend simulation confirmed `overlay_in_prompt=True`, `contains_style=True`, `contains_original_name=False`. Full unit passed: `.venv/bin/python -m pytest -m unit` -> 445 passed, 17 deselected. |
| 2026-05-31 | MaAI backchannel / unfinished follow-up gate regression | unit-only control-path change | pass | Latest real server log showed normal reply starting on unfinished tails such as `...よくさぁ` and `...関係が`; this pass changes participation gating and MaAI release cooldown only, without adding hot-path model work. Full unit passed: `.venv/bin/pytest -m unit -q` -> 508 passed, 17 deselected. Global ruff passed. Real browser latency should be rechecked with `make server-debug`. |
| 2026-05-31 | MaAI gesture audio speaking-state regression | unit-only control-path change | pass | Latest real server log showed MaAI `なるほど` setting server-side `playback_state=speaking`, causing the next user transcript to enter normal turn-taking/reply flow. Backchannel TTS now bypasses normal audio turn tracking and reply-output latency state. Full unit passed: `.venv/bin/pytest -m unit -q` -> 508 passed, 17 deselected. Global ruff passed. |
| 2026-05-31 | MaAI gesture audio lane extraction | unit-only control-path change | pass | Moved MaAI backchannel release out of TomoroSession into `GestureAudioEmitter`, which reads a runtime snapshot and sends gesture audio on the existing websocket without mutating conversation state. Focused unit passed: `.venv/bin/pytest -m unit tests/unit/test_gesture_audio.py tests/unit/test_maai_backchannel_tap.py tests/unit/test_smoke_maai_dialogue.py tests/unit/test_smoke_maai_material.py tests/unit/test_maai_backchannel_adapter.py -q` -> 23 passed. Full unit passed: `.venv/bin/pytest -m unit -q` -> 504 passed, 17 deselected. Global ruff passed. Real browser latency should be rechecked with `make server-debug`. |
| 2026-05-31 | Clock query filter and resumed speech stale cancel | unit-only control-path change | pass | Latest real server log showed `今何時` was recognized by STT but dropped as `low_audio_short_text`, while longer clock wording reached the prompt and answered from current local time. The same log showed VAD speech_end followed by resumed `listening` before reply output, but the unstarted reply was not cancelled. Clock queries now bypass the low-audio short filter, and `listening` cancels unstarted replies. Focused unit passed: `.venv/bin/pytest -m unit tests/unit/test_stt_filter.py tests/unit/test_streaming_tts_pipeline.py tests/unit/test_attention_mode.py tests/unit/test_participation.py tests/unit/test_phase885_session_runtime.py tests/unit/test_audio_turn_controller.py tests/unit/test_gesture_audio.py -q` -> 42 passed. Full unit passed: `.venv/bin/pytest -m unit -q` -> 505 passed, 17 deselected. Global ruff passed. |
| 2026-05-31 | Output lane / floor ownership characterization | unit-only control-path change | pass | Added explicit `OutputLane` boundaries for reply, initiative, gesture, stop ack, and future interrupting turns. This does not add hot-path model work or audio generation; it makes closed-loop floor ownership and log policy explicit. Focused unit passed: `.venv/bin/pytest -m unit tests/unit/test_audio_turn_controller.py tests/unit/test_phase10_session_contract.py tests/unit/test_gesture_audio.py tests/unit/test_phase10_candidate_command_runner.py tests/unit/test_phase5_tts.py tests/unit/test_phase105_session_runtime.py tests/unit/test_streaming_tts_pipeline.py -q` -> 53 passed. Real browser latency is unchanged by this unit-only boundary pass. |
| 2026-05-31 | MaAI backchannel threshold slight increase | unit-only control-path change | pass | Raised MaAI react backchannel default threshold from 0.45 to 0.50 after real conversation felt slightly too chatty. Cooldown, gesture audio lane, and normal turn ownership are unchanged. Focused unit passed: `.venv/bin/pytest -m unit tests/unit/test_maai_backchannel_adapter.py tests/unit/test_gesture_audio.py -q` -> 13 passed. Full unit passed: 516 passed, 17 deselected. Global ruff passed. `make smoke-maai-dialogue` still released the clear `0.7259804606437683` react cue with `threshold=0.5`. Real browser frequency should be rechecked with `make server-debug`. |
| 2026-05-31 | Low-audio transcript sentence acceptance | unit-only control-path change | pass | Latest real server log showed ambient STT text `大変良いと思いますよ私は` was recognized but dropped as `low_audio_short_text`, so UI LOG never received `transcript_final`. The low-audio short threshold was narrowed from 20 to 6 normalized chars: complete sentences such as `いいと思います` now pass, while very short low-audio fragments such as `たぶんね` still drop. Full unit passed: `.venv/bin/pytest -m unit -q` -> 519 passed, 17 deselected. Global ruff passed. Real browser UI LOG behavior should be rechecked with `make server-debug`. |
| 2026-06-01 | Client audio device picker | client-only UI routing | pass | Added browser-side input/output device selectors. `/ws`, TomoroSession, VAD/STT/TTS hot path, and playback telemetry contract are unchanged. Focused static client unit passed: `.venv/bin/pytest -m unit tests/unit/test_client_audio_devices.py -q` -> 4 passed. `node --check client/main.js` and `git diff --check` passed. Chrome layout smoke showed desktop selectors visible and mobile `bodyScrollWidth=390` for `viewportWidth=390`. Full unit currently has 2 pre-existing calendar-context failures in `tests/unit/test_phase88_context_snapshot.py`, unrelated to this client-only change. |
| 2026-06-01 | Research full-text follow-up speech | Tomoko smoke with real `tomoko-research-operator` subprocess | pass, `short_answer` 45 chars / `answer_reply_text` 549 chars | `logs/research-tomoro-session-real-smoke-fulltext.json` was generated with `_tools/smoke_research_tomoro_session_flow.py --speech '智子、今日の世界情勢について調べて' --answer-followup '結果を教えて' --command 'uv --directory /Users/seijiro/Sync/sync_work/by-llms/tomoko-research-operator run tomoko-research-mcp' --timeout-sec 180`. The change does not add online model work; it preserves operator `full_text` and feeds that to the existing precomputed reply/TTS path when the user asks for the result. Full unit still has the known 2 calendar-context failures in `tests/unit/test_phase88_context_snapshot.py`. |
| 2026-06-01 | Calendar future 30-day prompt context | ContextSnapshotBuilder calendar DB read window / limit | unit pass, real latency not measured | Calendar context now reads today through the next 30 days with `max_calendar_events=64` for deep context. This increases prompt context size when many calendar events exist but does not add model calls. Focused unit passed: `.venv/bin/pytest -m unit tests/unit/test_phase88_context_snapshot.py::test_deep_snapshot_reads_calendar_context tests/unit/test_phase88_context_snapshot.py::test_deep_snapshot_reads_all_future_30_day_calendar_context tests/unit/test_phase88_context_snapshot.py::test_tomoro_session_carries_calendar_context_into_short_followup -q` -> 3 passed. |
| 2026-06-01 | Calendar prompt overload hotfix | clock query selector + calendar long-term dedupe | unit pass, live recheck needed | Latest live log showed `今何時` entering deep calendar context and 42 calendar events being duplicated in `CALENDAR CONTEXT` and long-term memory. VOICEVOX returned chunks, but the LLM degenerated into repeated text and produced oversized TTS/audio queue overflow. Hotfix keeps clock queries out of calendar retrieval and avoids duplicating fresh calendar context into `long_term_memory` on the same turn. Focused related unit passed: 41 passed. Full unit still has the unrelated persona overlay `原作台詞` failure. |
| 2026-06-02 | VOICEVOX 16kHz output pitch check | `voicevox_tsumugi` / `voicevox_tsumugi_stream` outputSamplingRate 16kHz | focused unit pass, full unit pass, live recheck needed | Long Tomoko speech sometimes sounds low-pitched for the first ~10 seconds. To isolate browser resampling from 24kHz VOICEVOX WAV into the 16kHz AudioContext, central and edge VOICEVOX output sample rates were changed to 16kHz while leaving mic/VAD/STT at 16kHz. Focused config/VOICEVOX unit: 10 passed. Full unit: 613 passed, 23 deselected. |
| 2026-06-05 | VOICEVOX chunked default | `voicevox_tsumugi_chunked` / `/streaming_synthesis` / `chunk_min_accent_phrases=1` | focused unit pass, live latency not measured | Added a chunked VOICEVOX backend that parses multipart complete-WAV parts into `AudioChunkOut(data, sequence, is_last)` and switched central / edge defaults to it. This preserves the existing browser `decodeAudioData()` path while allowing first audio to arrive before full utterance synthesis. Focused VOICEVOX/config/prepare unit: 27 passed. Real first-chunk latency still needs `make server-debug` with the chunked engine running. |
| 2026-06-06 | VOICEVOX PR1823 stream contract | `voicevox_tsumugi_chunked` / PR1823 `audio/wav` stream / 24kHz mono / `segment_length=0.6` | first 1024.1ms / total 1652.6ms / focused unit pass | Updated the chunked backend for `async-voicevox` PR1823 on `http://127.0.0.1:50122`. The backend now accepts both legacy multipart complete-WAV parts and PR1823 single `audio/wav` stream, rewrapping streamed PCM segments as complete WAV chunks so the browser `decodeAudioData()` path remains unchanged. Direct backend smoke for `うん、わかった。少し待ってね。` returned 5 complete WAV chunks, all 24kHz mono. PR1823 rejects 16kHz `outputSamplingRate` with 422, so this chunked default uses 24kHz while the older comparison VOICEVOX backends keep their 16kHz pitch-check setting. |
| 2026-06-06 | VOICEVOX PR1823 first chunk reduction | `voicevox_tsumugi_chunked` / pending-delay removal / `segment_length=0.2` | 0.6s: first 1512.2ms / total 2283.6ms; 0.2s: first 166.0ms / total 2203.1ms | Removed Tomoko-side pending 1 chunk delay for PR1823 `audio/wav` stream and changed central / edge default `segment_length` to 0.2. Direct backend smoke against `http://127.0.0.1:50122` with text `うん、わかった。少し待ってね。`: 0.6s produced 5 chunks, 0.2s produced 15 chunks. Combined WAV artifacts: `logs/voicevox-pr1823-segment-compare/segment_0_6.wav` and `logs/voicevox-pr1823-segment-compare/segment_0_2.wav`; both are valid 24kHz mono WAV. Subjective audio quality was not judged in this run; generated files are available for local listening. |
| 2026-06-02 | Timer/alarm foundation review | DB row + TomoroSession gate + existing reply/TTS lane | unit/integration pass, live recheck needed | Reviewed uncommitted timer/alarm foundation. Focused unit passed: `uv run pytest -m unit tests/unit/test_timer_alarm_voice.py tests/unit/test_barge_in.py -q` -> 37 passed. Full unit passed: 611 passed, 23 deselected. Timer/alarm DB + smoke integration passed: 3 passed. No real browser first-audio latency measured; due notification playback should be checked with `make server-debug`. |
| 2026-06-02 | Timer/alarm preemptive notice policy | TomoroSession due reducer / audio stop command path | unit/integration pass, live recheck needed | Timer/alarm due now defers while user is listening, fails immediately when output is unavailable, and preempts active Tomoko reply/playback with `cancel_reply_generation` + `audio_control stop` before starting the due notice. Focused unit passed: 23 passed; related unit passed: 39 passed; timer/alarm integration passed: 3 passed. No browser first-audio latency measured. |
| 2026-06-05 | World observation operator collection | `tomoko-research-operator` subprocess / `world.observe` / `informations/work` save | focused unit pass, live Chrome smoke not run | Added a background/manual collection path that calls sibling `tomoko-research-mcp` instead of relying on Codex / Computer Use. This does not add online conversation work or ContextSnapshotBuilder work. Operator focused unit passed: 17 passed. Tomoko focused unit passed: 18 passed. Focused ruff passed. Live Chrome / Perplexity collection and strict ingest dry-run still need a logged-in browser smoke. |
| 2026-06-05 | World observation operator live collection | `make information-collect-world` with Chrome CDP / Perplexity provider timeout 240s | pass, 12,671 chars | Initial failure was the operator provider timeout still using its 90s default, not the Make subprocess timeout. After passing `TOMOKO_WORLD_OBSERVATION_PROVIDER_TIMEOUT_SEC=240` and accepting rendered `innerText` labels including full-width colons, live Make collected `informations/work/2026-06-05-world-observation.md`. Strict validator passed with `issues=[]`; `make information-ingest-dry-run` reported `processed=1` and `would_ingest`. |
| 2026-06-05 | World observation ingest fallback | LLM normalizer failure -> deterministic rendered-text fallback | ingest pass, 8 items | `make information-ingest-once` initially failed because the memory-extraction backend exceeded context length on a 29,913-char rendered body. The deterministic fallback now accepts topic-only headings and `観測1：...` titles plus `source_hint：` labels. Retry archived `informations/archived/2026-06-05/2026-06-05-world-observation.md`, saved 8 representative items, and `make information-interpret-once` completed with `interpreted=10 error_count=0`. |
| 2026-06-05 | World observation long generation timeout | `make information-collect-world` timeout defaults | unit pass, live rerun pending | Perplexity can still be generating at the previous 240s limit. Raised both outer MCP and provider response timeout defaults to 600s and made the operator defer `blocked by provider` classification while `isGenerating=true`. `make -n information-collect-world` now passes both timeout env vars as 600. Focused Tomoko unit: 11 passed. Focused operator unit: 20 passed. Focused ruff and diff checks passed. |
| 2026-06-05 | World observation numbered fallback | rendered `1. title` headings after LLM normalizer context failure | ingest pass, 8 items | A 11,688-char Perplexity artifact used topic-only headings plus numbered titles like `1. ガザ情勢...`; the previous fallback only recognized `観測1：...`. Added deterministic parsing for `1. title` / `1) title` / `1：title`. Retry: strict validator passed, `make information-ingest-once` archived the file with 8 items, and `make information-interpret-once` completed with `interpreted=10 error_count=0`. Focused unit: 27 passed. |
| 2026-06-06 | Attention timeout extension | TomoroSession idle attention decay | unit pass, no hot-path model work | Extended default attention decay from engaged 20s / cooldown 8s to engaged 120s / cooldown 60s. Timeout still advances only on idle, playback-idle silent audio chunks, so model/TTS/STT latency path is unchanged. Focused attention unit: 10 passed. |
| 2026-06-07 | dflash Gemma 4 26B/31B probe | `localhost:8082` 26B / `localhost:8081` 31B OpenAI-compatible streaming | 26B config avg first content 3645.9ms / total 3679.7ms; 31B config avg first content 8616.5ms / total 8661.5ms | After switching config from LM Studio remote `192.168.11.66:1234` to local dflash ports, direct streaming probes showed early `reasoning_content` but delayed `content`. 26B config name `gemma-4-26b-a4b-it-mlx`: first any token avg 362.8ms, first content avg 3645.9ms, total avg 3679.7ms over 3 runs. 31B config name `gemma-4-31b-it-mlx`: first any token avg 990.3ms, first content avg 8616.5ms, total avg 8661.5ms over 3 runs. dflash canonical model IDs were slightly faster to first any token, but first content remained about 3604.0ms for 26B and 8652.9ms for 31B. Current Tomoko `LMStudioBackend` ignores `delta.reasoning_content`, so user-visible LLM latency should compare first content, not first any token. |
| 2026-06-07 | dflash no-think request fix | `chat_template_kwargs.enable_thinking=false` for 26B/31B dflash backends | 26B first 438.0ms / total 504.8ms; 31B first 1372.2ms / total 2085.3ms | dflash accepts per-request `chat_template_kwargs = { enable_thinking = false }`. `think=false` / `thinking=false` did not stop `reasoning_content`, but `chat_template_kwargs` removed reasoning chunks and made `delta.content` arrive immediately. Tomoko `LMStudioBackend` now forwards backend-specific `chat_template_kwargs`, and central 26B/31B dflash config sets no-think explicitly. Focused unit passed: 13 passed. Focused ruff passed. Runtime probe used `InferenceRouter` / `LMStudioBackend` against live `localhost:8082` and `localhost:8081`. |
| 2026-06-07 | dflash no-think server restart check | dflash `--chat-template-args '{"enable_thinking": false}'` plus Tomoko request `chat_template_kwargs` | direct 26B avg first 276.6ms / total 369.1ms; direct 31B avg first 306.7ms / total 984.0ms; Tomoko 26B first 541.4ms / total 643.1ms; Tomoko 31B first 1064.8ms / total 1723.5ms | After restarting both dflash servers with server-side no-think, direct OpenAI-compatible probes produced no `reasoning_content` chunks even without per-request kwargs. Three-run direct probe with Tomoko's request kwargs: 26B `localhost:8082` first content avg 276.6ms / total avg 369.1ms; 31B `localhost:8081` first content avg 306.7ms / total avg 984.0ms. One Tomoko `InferenceRouter` probe returned 26B conversation `了解。` first 541.4ms / total 643.1ms and 31B memory_extraction `了解。` first 1064.8ms / total 1723.5ms. Compared with the earlier dflash thinking run, this is a large improvement. Compared with LM Studio notes, 26B is roughly comparable to slightly faster on direct short probes, and 31B is clearly faster than the prior 31B LM Studio short-response range. |
| 2026-06-07 | LM Studio vs dflash 26B/31B short and long prompt bench | LM Studio `192.168.11.66:1234` vs dflash `8082`/`8081`, no-think | warm short: LM26 first 72.4ms / total 73.0ms, DF26 first 223.4ms / total 350.4ms, LM31 first 820.5ms / total 821.8ms, DF31 first 290.5ms / total 1028.9ms; warm medium: LM26 first 879.6ms / total 1839.4ms, DF26 first 341.2ms / total 2903.0ms, LM31 first 4803.6ms / total 13033.4ms, DF31 first 480.7ms / total 10657.3ms; 10k Tomoko-like: LM empty, dflash warm first 251.7ms/363.3ms | Direct OpenAI-compatible benchmark with 3 runs per target. Artifacts: `logs/llm-lmstudio-dflash-short-long-20260607.json` and `logs/llm-lmstudio-dflash-medium-20260607.json`. Short prompt was `了解。`. Medium Tomoko-like prompt used `system_chars=2705`, `message_chars=398`, 9 messages. Full Tomoko-like prompt used `system_chars=9966`, `message_chars=1304`, 25 messages. Warm averages are run 2-3 to show prefix/cache steady state. For short prompts, LM Studio 26B is fastest; dflash 31B has faster first content but slower total than LM Studio 31B. For medium long prompts, dflash improves first content markedly, especially 31B. For the 10k Tomoko-like prompt, LM Studio returned HTTP 200 but no `delta.content` for both 26B and 31B, while dflash completed; warmed dflash 26B first content 251.7ms / total 3541.3ms and dflash 31B first content 363.3ms / total 9402.0ms. |
| 2026-06-07 | dflash startup persona prefix warm-up | startup `ThinkFastMode.system_prompt` prefix warm-up for no-think 26B/31B dflash backends | unit pass, live startup latency not measured yet | Added startup-only dflash prompt prefix warm-up after the standard conversation backend warm-up. It targets only `lm_studio` backends with `chat_template_kwargs.enable_thinking=false`, dedupes configured roles by backend, sends Tomoko's fixed persona / overlay system prefix plus a short warm-up user message, and limits generation to `max_tokens=4`. This is intended to prefill dflash prefix cache for the stable front of Tomoko prompts. Focused unit: 17 passed. Full unit: 633 passed, 23 deselected. Real startup overhead and first live conversation benefit still need `make server-debug` measurement. |
| 2026-06-07 | dflash static prompt prefix ordering probe | `STATIC CONTEXT USAGE RULES` before dynamic time/context blocks | 26B raw proposed first 4535.2ms vs baseline 5781.8ms in hand prompt; actual code first 4857.2-5924.1ms; 31B no clear win | Moved fixed context usage rules into `ThinkFastMode.system_prompt`, before `CURRENT LOCAL TIME`, so startup dflash warm-up includes persona / overlay / static context rules. Dynamic blocks now avoid repeating fixed explanations. Raw 26B hand prompt after dflash restart showed warm-up-following first medium call improved from first 5781.8ms / total 8040.5ms to first 4535.2ms / total 6394.1ms. Actual `ThinkFastMode` prompt after implementation measured first 5924.1ms / total 8142.2ms, and a repeat restart measured first 4857.2ms / total 7259.2ms; warmed full-prompt repeats fell to first 218.9-352.8ms. 31B single restart comparison was noisy and did not show a win: baseline first 25103.4ms / total 30826.2ms, proposed first 28152.6ms / total 34293.2ms. Conclusion: fixed-prefix ordering is architecturally useful and helps some 26B cold-ish raw calls, but a significant live benefit is not proven yet without multi-restart statistics / `make server-debug` first-audio measurement. |
| 2026-06-07 | dflash 26B sequential conversation prompt-shape simulation | current dynamic-system prompt vs proposed user-side turn metadata | current avg first 3671.6ms / total 6056.7ms; proposed avg first 3053.7ms / total 5178.0ms | Ran 6 sequential OpenAI-compatible streaming conversation turns against no-think dflash 26B after separate dflash restarts. Current structure kept dynamic time/context in the system prompt. Proposed raw payload kept the system prompt static and moved turn metadata/current time/task context/current utterance into the final user message. Proposed was faster on every turn: first content improved by 345.7-1023.5ms, average first content improved 618.0ms, and average total improved 878.8ms. Semantics stayed coherent across the 6-turn sample; no obvious context break or persona collapse was observed. Both structures still tended to ask follow-up questions. Artifacts: `logs/dflash-26b-conversation-sim-current-20260607.json`, `logs/dflash-26b-conversation-sim-proposed-user-metadata-20260607.json`. |
| 2026-06-07 | dflash 26B runtime ThinkFastMode user-side turn metadata | real config / real PostgreSQL context / real dflash 26B | before restart baseline avg first 5142.1ms / total 7715.7ms; after avg first 4697.1ms / total 7064.3ms | Implemented the prompt-shape change in production `ThinkFastMode`: system prompt now stays at persona / overlay / `STATIC CONTEXT USAGE RULES`, while `CURRENT LOCAL TIME`, persona slice, response directive, calendar/research/task context, memory prompts, and current utterance are wrapped into the final user message. Runtime simulation script `_tools/bench_runtime_thinkfast_conversation.py` used `config/central_realtime.toml`, `InferenceRouter`, `ContextSnapshotBuilder`, real PostgreSQL stores, and live no-think dflash 26B. It wrote dedicated `conversation_sessions` / `conversation_logs` rows so each subsequent turn could read DB context. Restarted dflash before before/after runs. Improvement: avg first content -445.0ms, avg total -651.4ms. Per-turn deltas were mixed: first content +199.8, -1383.3, -1286.6, +88.2, -423.7, +135.6ms; total -1548.7, -2370.0, -2936.5, +2114.0, -246.8, +1079.4ms. Semantics stayed coherent with no obvious persona/context collapse. Artifacts: `logs/runtime-thinkfast-26b-before-user-metadata-restarted-20260607.json`, `logs/runtime-thinkfast-26b-after-user-metadata-restarted-20260607.json`. |
| 2026-06-07 | `/ws` human-perceived voice latency smoke | macOS `say` -> 16kHz mono float32 chunks -> `/ws` -> VAD/STT/dflash 26B/VOICEVOX | voice-end to transcript 1357.2ms; voice-end to first reply_text 7150.3ms; voice-end to first binary audio 7821.3ms | Added `_tools/smoke_ws_voice_latency.py` and `make smoke-ws-voice-latency` to replay generated speech plus trailing silence through the real browser WebSocket contract. Input: `トモコ、短く返事して。`, voice `Kyoko`, realtime 512-sample chunk pacing, trailing silence 1200ms. STT recognized `智子短く返事して`; reply was `了解。短くいくね。`; 10 binary audio chunks / 90,040 bytes returned. Server trace: speech segment 2900.8ms, STT elapsed 488.0ms, context build 19.0ms, first reply_text speech-end 6320.3ms, first audio speech-end 6989.7ms, TTS start to first audio 449.1ms. Tool-side voice-end to first audio was 7821.3ms because it measures from the last voiced chunk sent, before the VAD silence window completes. dflash log showed this live prompt was a 1532-token prefix-cache miss with ~5.0s prefill, so the dominant cost was LLM prefill rather than VAD/STT/TTS. Artifact: `logs/ws-voice-latency-20260607-say-short.json`; input WAV: `logs/ws-voice-latency-20260607-say-short-input.wav`. |
| 2026-06-07 | `/ws` three-turn voice latency smoke | macOS `say` 3 utterances over one `/ws` session with playback telemetry | turn1 first audio 7819.0ms; turn2 8030.0ms; turn3 9359.6ms | Extended `_tools/smoke_ws_voice_latency.py` to support `--scenario three-turn` / repeated `--turn-text`, per-turn artifacts, playback telemetry emulation, and an inter-turn pause. Default three-turn input was `トモコ、短く返事して。` -> `うん、今の速度感はどう？` -> `ありがとう。もう一言だけお願い。`. STT recognized `智子短く返事して` / `うん今の速度感はどう` / `ありがとうもう一言だけお願い`; replies were `了解。これくらいでいい感じ？` / `んー、今の感じはどうかな？ちょっと速すぎた？` / `えー、そんなに急いでどうしたの？何かお手伝いする？`, so the sequence was semantically coherent. Tool-side voice-end to transcript was 1355.4ms / 1180.8ms / 1255.1ms; voice-end to first reply_text was 7102.3ms / 7189.8ms / 7365.0ms; voice-end to first binary audio was 7819.0ms / 8030.0ms / 9359.6ms. dflash startup warm-up showed fixed-prefix hits such as 804/808 restored tokens, but the live conversation prompts still missed prefix cache: 1338 / 1337 / 1155 prompt tokens each restored 0.0 and spent about 4.9-5.3s in prefill. Conclusion: warm-up helps stable prefix requests, but it does not eliminate full live prompt prefill yet. Artifact: `logs/ws-voice-latency-20260607-three-turn-final.json`. Turn 2/3 still included `barge_in` events under a 1500ms inter-turn pause, so a stricter post-playback mode should compare 3000-5000ms pauses. |
| 2026-06-07 | `/ws` time-return prompt order A/B | 4 turn scenario: short reply -> speed feeling -> current time -> return to speed topic | baseline avg first audio 6257.6ms; time-last avg 6002.1ms; topic-hint avg 8522.9ms | Tested whether moving `CURRENT LOCAL TIME` behind the conversation and current user utterance preserves time semantics while improving cache/prefill behavior. Scenario inputs: `トモコ、短く返事して。` -> `うん、今の速度感はどう？` -> `ところで、今何時？` -> `ありがとう。さっきの速度の話に戻って、もう一言だけお願い。`. Baseline answered time correctly (`午後1時19分`) but failed to return to speed topic (`えー、また？...`); avg voice-end to first binary audio 6257.6ms. Moving current utterance before turn context and putting current time last preserved time answering (`午後1時22分`) and improved average first audio to 6002.1ms, but still failed topic return. Adding a dynamic `TOPIC RETURN HINT` for explicit `さっきの...話に戻って` cues fixed topic return (`今のテンポ、ちょうどいい感じかな？`) and kept time answering (`午後1時31分`), but this single run worsened average first audio to 8522.9ms. dflash still showed full live prompt cache misses (`restored 0.0`) in these runs. Conclusion: keep current-utterance-first / time-last ordering; topic-return hint improves semantics but needs more runs before claiming latency benefit. Artifacts: `logs/ws-voice-latency-20260607-time-return-before.json`, `logs/ws-voice-latency-20260607-time-return-after.json`, `logs/ws-voice-latency-20260607-time-return-after-v4.json`. |
| 2026-06-07 | `/ws` time-last without topic hint recheck | removed `TOPIC RETURN HINT`; current utterance first and current time last only | avg first audio 6413.0ms | Removed the topic-return hint because this probe is meant to compare latency with baseline-level semantics, not to add semantic steering. Same 4 turn inputs as the previous time-return probe. The run preserved time answering (`今は午後3時56分だよ`) and turn 4 stayed broadly in the speed/shortness topic (`これくらい短めがちょうどいい感じ？`) without any special hint. Voice-end to first binary audio was 6135.8ms / 7262.6ms / 5299.0ms / 6954.7ms, average 6413.0ms. Compared with the original baseline average 6257.6ms, this run was +155.4ms slower; compared with the earlier time-last-only run 6002.1ms, it was +410.9ms slower. Conclusion: the time-last order did not break time semantics, but the latency difference is small/noisy rather than proven faster. dflash live prompts still had full prefill cache misses (`restored 0.0`). Artifact: `logs/ws-voice-latency-20260607-time-return-no-topic-hint.json`. |
| 2026-06-07 | dflash live prefix cache restoration | saved LLM user prompt content + same-session-only active context + fast context budget 50ms | runtime avg first 3409.6ms / total 5551.5ms; `/ws` turn3 first audio 2938.6ms | Step1 raw dflash A/B confirmed the cause: when turn2 history stored the previous user as raw transcript, cached tokens stayed 0; when it preserved the previous wrapped user content, turn2 restored 3729 cached tokens and elapsed dropped to 1054.2ms. Artifact: `logs/dflash-prefix-step1-history-shape-20260607-161119.json`. Implemented `conversation_logs.llm_prompt_content` as derived prompt audit data and made `ThinkFastMode` use it for prior user turns while keeping `transcript` raw. Active sessions now use same-session context without global recent supplementation, same-session context cache is disabled, and fast context budget is 50ms so baseline turns do not timeout. Runtime 6-turn real config / real PostgreSQL / live dflash 26B after the fix: recent turns 12 / 2 / 4 / 6 / 8 / 10, avg first text 3409.6ms, avg total 5551.5ms; turn3-6 first text 2092.9 / 1954.9 / 2217.4 / 2051.6ms. dflash logs showed repeated prefix hits from turn3 onward: 1602/2019, 2015/2436, 2432/2859, 2855/3282 with restored prefill. Artifact: `logs/runtime-thinkfast-26b-step3-fast50-cache-20260607.json`. Real `/ws` three-turn smoke on port 8010: first audio 4705.8 / 7624.5 / 2938.6ms; turn3 transcript-to-first-reply-text 1076.6ms; dflash turn3 hit 1534/1916 and restored 1926.2 tok/s. Artifact: `logs/ws-voice-latency-20260607-162554.json`. Focused/perf/full verification: ruff pass, focused unit 54 passed, context perf 1 passed, full unit 640 passed / 23 deselected. |
| 2026-06-07 | PostgreSQL connection pool for context hot path | `psycopg_pool.AsyncConnectionPool` / same-session context read / fast budget 20ms | writer read before warm 7.2-9.6ms -> after warm avg 0.936ms; runtime avg first 4150.2ms / total 5764.1ms | Same-session SQL was already fast: `EXPLAIN (ANALYZE, BUFFERS)` used `conversation_logs_session_recorded_at_idx` and executed in 0.077ms; reused-connection query avg was 0.469ms / max 1.899ms. The old writer created a new PostgreSQL connection per call, measuring first 24.525ms and warm 7.2-9.6ms. Added official `psycopg_pool.AsyncConnectionPool` and routed conversation / ambient / session stores through `pooled_connection(dsn)`. After pool warm-up, `PostgresConversationLogWriter.read_recent_turns_for_session()` measured avg 0.936ms / max 1.217ms, with first pool-open call 16.378ms. Fast context budget was restored from 50ms to 20ms. Runtime real config / real PostgreSQL / live no-think dflash 26B artifact `logs/runtime-thinkfast-26b-dbpool-fast20-20260607.json`: recent turns stayed 12 / 2 / 4 / 6 / 8 / 10, context elapsed 15.65 / 11.40 / 11.76 / 20.28 / 20.28 / 21.17ms, avg first text 4150.2ms, avg total 5764.1ms. dflash prefix hits continued from turn3 onward: 1579/1992, 1988/2407, 2403/2824, 2820/3238. Focused unit/perf passed: 56 passed + perf 1 passed; full unit 642 passed / 23 deselected; focused ruff pass. |
| 2026-06-07 | Session scoped calendar/task context cache | active session `calendar_events` / `task_ledger` cache keyed by session_id | real DB context build 19.317ms -> 0.117ms -> 0.090ms; new session 10.487ms | Added session scoped context cache for `calendar_events` and `task_ledger` only when `active_session_id` is present. `active_session_id=None` still uses the previous TTL cache. Cache keys include session_id, and changing session_id prunes old session entries, so concurrent/awaiting builds cannot reuse another session's calendar/task values. `same_session_turns` remains uncached. `task_ledger` session cache is invalidated when `task_ledger_update_finished` records `created` or `completed`. Real DB microbench with `PostgresCalendarEventStore` / `PostgresTaskLedgerStore`: same session turn1 elapsed 19.317ms with cache `calendar_events=False`, `task_ledger=False`; turn2 elapsed 0.117ms with both true; turn3 elapsed 0.090ms with both true; new session elapsed 10.487ms with both false. Runtime dflash 26B simulation artifact `logs/runtime-thinkfast-26b-session-context-cache-20260607.json`: avg first text 2479.1ms / avg total 4794.8ms. Focused unit/perf passed: 65 passed + perf 1 passed; full unit 646 passed / 23 deselected; focused ruff pass. |
| 2026-06-07 | Stable structured context prompt ordering | final user message order: stable structured context -> current utterance -> dynamic/recall context | fast: first 2249.5 -> 2241.3ms, total 4306.8 -> 4052.8ms; deep+tasks: first 7619.5 -> 7273.1ms, total 9880.5 -> 9137.8ms | Reordered `ThinkFastMode` current user message so persona slice / calendar / task context appear under `SESSION STABLE STRUCTURED CONTEXT`, followed by `CURRENT USER UTTERANCE`, then response directive / short memory / long memory / research / current time under `TURN DYNAMIC / RECALL CONTEXT`. Fast real DB / dflash 26B had no calendar/task rows in the prompt and was effectively neutral: current avg first 2249.5ms / total 4306.8ms; stable-first avg first 2241.3ms / total 4052.8ms. Deep with calendar 41 / task 0: current avg first 6303.1ms / total 9678.5ms; stable-first avg first 6710.4ms / total 9286.5ms, so first worsened but total improved. Deep with calendar 41 and 3 temporary benchmark tasks: current avg first 7619.5ms / total 9880.5ms; stable-first avg first 7273.1ms / total 9137.8ms. Temporary tasks used `source='codex_prompt_order_bench'` and were deleted after measurement (`bench_task_rows 0`). No obvious semantic collapse or loss of current-turn tracking was observed in the 6-turn samples. Artifacts: `logs/runtime-thinkfast-26b-prompt-order-current-20260607.json`, `logs/runtime-thinkfast-26b-prompt-order-stable-first-20260607.json`, `logs/runtime-thinkfast-26b-prompt-order-current-deep-20260607.json`, `logs/runtime-thinkfast-26b-prompt-order-stable-first-deep-20260607.json`, `logs/runtime-thinkfast-26b-prompt-order-current-deep-with-tasks-20260607.json`, `logs/runtime-thinkfast-26b-prompt-order-stable-first-deep-with-tasks-20260607.json`. Focused unit 30 passed; full unit 646 passed / 23 deselected; context perf 1 passed; focused ruff pass. |
| 2026-06-07 | LM Studio vs dflash 26B warmed Tomoko-like prompt | OpenAI-compatible direct stream / same `ThinkFastMode` prompt / full prompt warmup | LM Studio 26B warmed avg first 877.1ms / total 1220.6ms; dflash 26B warmed avg first 253.1ms / total 3680.0ms | Built a Tomoko-like prompt from current `ThinkFastMode` using real DB deep context plus 8 synthetic same-session history turns with prior user `llm_prompt_content` shape. Prompt stats: 10 messages, total 16,937 chars, system 1,508 chars, current user 3,099 chars, snapshot source counts recent_turns 12 / lexicon_terms 4 / persona_slice 1 / calendar_events 41 / task_ledger 0. Each backend received one full-prompt warmup call and then 3 measured warmed calls with identical messages, `temperature=0`, `max_tokens=160`, and `chat_template_kwargs.enable_thinking=false`. LM Studio 26B (`localhost:1234`, `gemma-4-26b-a4b-it-mlx`) warmup first 10183.8ms / total 10554.5ms, warmed runs first 968.1 / 881.6 / 781.5ms, total 1333.4 / 1260.9 / 1067.5ms. dflash 26B (`localhost:8082`, `mlx-community/gemma-4-26b-a4b-it-4bit`) warmup first 28564.5ms / total 32970.4ms, warmed runs first 262.1 / 238.6 / 258.7ms, total 3790.5 / 3447.6 / 3801.8ms. dflash server log showed warmed prefix hits `10401/10405 tokens`, with prefill around 0.2s and restored prefill >42k tok/s, so the warmed first-content advantage is from prefix cache restoration. LM Studio generated a much shorter reply (28 chars avg) than dflash (99 chars avg), so total latency is not apples-to-apples; for perceived turn start, dflash was about 624ms faster on average. Artifact: `logs/lmstudio-dflash-26b-tomoko-warmed-20260607.json`. |
| 2026-06-12 | Gemma 4 E2B MLX short reaction bench | `mlx-community/gemma-4-e2b-it-4bit` via `mlx_vlm`, max_tokens=8, 10 short backchannel prompts | warm first_text p50 400.9ms / p95 475.9ms; warm total p50 412.5ms / p95 490.8ms | Added `_tools/bench_short_reaction_mlx.py` to measure Neuro-style short reactions such as `うん` / `マジで？` / `あー、それね`. Model load took 7683.1ms, so startup warm-up / resident process is required. After 2 warm-up runs, 10/10 samples produced first text within 500ms and 10/10 completed within 500ms. Outputs included `それは大変`, `そうかも`, `速いね`, `意外だね`, `了解`. `mlx_lm` runner was also tried with the same HF model but failed to load due incompatible weight keys, so the configured `gemma_mlx` / `mlx_vlm` path is the usable local route. Artifact: `logs/short-reaction-mlx-20260612/short-reaction-mlx_vlm.json`. This measures LLM text only; 0.5s to audible speech still requires cached/pre-generated short reaction audio or a very fast TTS lane. |
| 2026-06-12 | Qwen3 1.7B short persona LoRA Step 1 | `Qwen/Qwen3-1.7B-MLX-4bit`, `enable_thinking=False`, base persona + overlay prompt following check | stopped: `EMOTION:` 0/8, `<think>` leak 8/8 | Initial model load/download took 55702.9ms. Average generation elapsed was 1778.3ms, max 2042.7ms, but quality/format failed before latency mattered. The model accepted `enable_thinking=False` in `tokenizer.apply_chat_template`, yet every output began with `<think>` reasoning, often English or Chinese mixed, and no output started with `EMOTION:<label>`. Because Tomoko's audio response lane requires first-line `EMOTION`, valid emotion labels, Japanese body, and no reasoning leakage, the experiment stopped at Step 1. `loras/short/` was not created, and LoRA/fuse were not run. Artifact: `logs/qwen3-short-lora-20260612/step1_base_non_thinking.json`. |
| 2026-06-12 | Gemma 4 E2B OptiQ short persona LoRA | `mlx-community/gemma-4-e2b-it-OptiQ-4bit`, non-thinking rendered `text` JSONL, `loras/short/adapters` | adapter short avg 451.7ms / 4 of 5 under 500ms; dequantized fused short avg 689.1ms | `mlx_lm.load` / `mlx_lm.generate` / `mlx_lm lora` all succeeded. Default chat template injected thinking, but `enable_thinking=False` produced a non-thinking lane. Because `mlx_lm` chat datasets do not pass this flag, `loras/short/data` was rendered to `text` JSONL with `enable_thinking=False` before training. Final adapter eval: 12 prompts, `EMOTION:` 12/12, valid emotion 12/12, basic pass 11/12. Short adapter speed with max_tokens=16: avg 451.7ms, max 801.2ms, 4/5 under 500ms. Normal quantized fuse was not reliable (`EMOTION:` 7/10, valid emotion 6/10) and is kept as `loras/short/fused_model_quantized_unreliable`. `--dequantize` fused model was reliable enough (`EMOTION:` 10/10, valid emotion 10/10, basic pass 9/10) but larger and slower: canonical `loras/short/fused_model`, 8.7GB, short avg 689.1ms, 1/5 under 500ms. Artifacts: `logs/gemma-e2b-optiq-short-lora-20260612/step2b_adapter_eval_augmented.json`, `step2d_adapter_short_speed.json`, `step3_fused_eval.json`, `step3b_fused_dequantized_eval.json`, `step3c_fused_short_speed.json`. |
| 2026-06-13 | PostgreSQL LISTEN/NOTIFY delivery microbench | local PostgreSQL via `config/central_realtime.toml`, separate async listener / producer connections | paced p50 0.701ms / p95 1.840ms; burst p50 0.310ms / p95 0.805ms | Added `_tools/bench_postgres_notify_latency.py` to measure `pg_notify` payload timestamp to listener receive time with warm-up samples discarded. Paced run: `uv run python _tools/bench_postgres_notify_latency.py --config config/central_realtime.toml --samples 500 --warmup 20 --interval-ms 1 --output logs/postgres-notify-latency-20260613.json`; avg 0.856ms, p99 3.566ms, max 7.170ms, `notify_execute_ms` avg 0.921ms. Burst run: `--samples 2000 --warmup 50 --interval-ms 0 --output logs/postgres-notify-latency-20260613-burst.json`; avg 0.407ms, p99 2.171ms, max 11.493ms, `notify_execute_ms` avg 0.435ms. The measured app-to-listener signal path is sub-ms to low-ms in normal cases; occasional scheduler/DB tail outliers can reach ~10ms. |
| 2026-06-13 | Main conversation LLM format/latency simulation | LM Studio `localhost:1234`, Tomoko `ThinkFastMode` prompt, LFM2.5 8B A1B / Gemma 4 E4B / Gemma 4 26B | warm: LFM content 0/4; E4B first 338.5ms / total 1391.6ms / format 3/4; 26B first 570.7ms / total 1695.6ms / format 4/4 | Added `_tools/bench_main_llm_format.py` and ran `uv run python _tools/bench_main_llm_format.py --output logs/main-llm-format-20260613.json --repeat 2 --max-tokens 96`. All three models used the same Tomoko base persona + overlay and recent-turn shaped prompt. `LiquidAI/LFM2.5-8B-A1B-MLX-4bit` was represented by LM Studio model id `lfm2.5-8b-a1b-mlx`; it returned `content=""` and 0 streamed content chunks in all 8 runs, so Tomoko would emit no reply with the current `LMStudioBackend`. Direct non-stream probes confirmed the text was emitted only as `reasoning_content`; `chat_template_kwargs.enable_thinking=false` / `think=false` did not change this, and even `max_tokens=256` stayed all reasoning tokens. Gemma 4 E4B with `enable_thinking=false` produced `EMOTION:` 8/8 and valid emotion 8/8, but 2/8 conservative format checks failed because the body repeated model names `26B/E4B/LFM`. Gemma 4 26B A4B produced `EMOTION:` 8/8, valid emotion 8/8, no reasoning leak, no ASCII body, and better conversational framing, at about +232ms warm first delta vs E4B in this run. Cold first turn included LM Studio model load outliers: E4B first 10.4s, 26B first 12.7s. |
| 2026-06-13 | Turn-taking semantic LLM bench | `LiquidAI/LFM2-350M-Extract` vs `mlx-community/gemma-4-e2b-it-4bit`, 16 Japanese partial/final utterance cases | LFM compact first 290.1ms but strict shape 0/16; Gemma E2B compact first 613.3ms / strict shape 16/16 / accuracy 16/16 | Added `_tools/bench_turn_taking_semantic_llm.py` and `tests/unit/test_turn_taking_semantic_llm_bench.py`. Structured prompt matches Tomoko `chat_stream_structured`; compact prompt is a short extract-only JSON prompt. LFM structured: parse 0/16, strict shape 0/16, avg first 343.7ms, avg total 1059.5ms. LFM compact: parse 2/16, strict shape 0/16, avg first 290.1ms, avg total 734.4ms. Gemma E2B structured: strict shape 16/16, finished accuracy 15/16, avg first 889.3ms, avg total 1071.4ms. Gemma E2B compact: strict shape 16/16, finished accuracy 16/16, avg first 613.3ms, avg total 795.2ms. Artifacts: `logs/turn-taking-semantic-lfm2-350m-extract-structured-20260613.json`, `logs/turn-taking-semantic-lfm2-350m-extract-compact-20260613.json`, `logs/turn-taking-semantic-gemma4-e2b-structured-20260613.json`, `logs/turn-taking-semantic-gemma4-e2b-compact-20260613.json`. |
| 2026-06-18 | Tomoko v2 scaffold smoke | root v2 DTO/runtime/process scaffold, fake model/TTS unit path, CLI hooks | live first content/audio not measured | Implemented V2.0-V2.20 scaffold boundaries without starting external Apple Speech / VOICEVOX / LLM / OCR runtimes. `make check` passed with 28 unit tests and ruff. `make v2-info-once` wrote a sample calendar DTO map, `make v2-initiative-sim` produced fire markers from 4s silence onward in the synthetic high-pressure scenario, `make v2-floor-bench` produced continue decisions for 600-1500ms pause candidates, `uv run python -m server.runtime readiness` printed external readiness expectations, and `make v2-report-latest` generated `reports/v2-latest.html`. Because no real `/ws` model/TTS conversation was run, first content / first audio / total latency remain acceptance items for the first live V2.20 smoke. |
| 2026-06-18 | Tomoko v2 real runtime launcher + OCR smoke | v1-equivalent Makefile targets, dflash/VOICEVOX launcher, macOS OCR runtime | first content/audio not measured; OCR smoke succeeded | Added root `llm-run` / `voicevox-run` / `tmux-runtime` / `v2-runtime-ready` / `v2-llm-tts-smoke` / `v2-ocr-smoke`. `uv run python -m server.runtime readiness` showed OCR binaries available (`screencapture`, `tesseract`, `osascript`) but DB, dflash `8081/8082`, and VOICEVOX `50122` were not currently running. `make v2-ocr-smoke` captured a screenshot, extracted 2472 OCR chars, and merged OS metadata including PyCharm and a YouTube URL/title; activity label was `watching_video`. dflash binary, `v1/loras/lora/fused_model`, and the sibling VOICEVOX command existed. Real LLM/TTS first content / first audio should be measured after `make tmux-runtime` brings endpoints up. |
| 2026-06-18 | Tomoko v2 `/ws` real executor contract smoke | FastAPI `/ws` prompt event with fake executor, client binary WAV playback path, OCR rerun | live first content/audio not measured; unit contract passed | Wired root `/ws` prompt events to `PromptExecutor` so the same WebSocket can emit `model_delta` / `model_complete`, binary WAV chunks, `audio_complete`, and `prompt_complete`. Added a unit smoke that injects fake chat/TTS backends and verifies the server contract without external runtimes; `make check` passed with 31 unit tests and ruff. Client now decodes server binary WAV chunks for playback. OCR rerun extracted 2422 chars and OS metadata from Google Chrome/Gmail; LLM and VOICEVOX were still not running, so perceived first content / first audio remains pending until `make tmux-runtime` plus `make v2-llm-tts-smoke`. |
| 2026-06-18 | Tomoko v2 audio `/ws` conversation smoke with sidecar runtime readiness | Apple Speech sidecar readiness, Vision OCR sidecar, fake audio conversation smoke | live first content/audio not measured; fake smoke passed | Added root Apple Speech STT and Vision.framework OCR sidecars. `uv run python -m server.runtime readiness` reported Apple Speech `binary/source/plist/swiftc=true` and OCR `screencapture/vision_ocr/tesseract/osascript=true`; dflash `8081/8082` and VOICEVOX `50122` were still not running. `make v2-ocr-smoke` used the Vision-first OCR path and extracted 2739 chars from the current screen with OS metadata. `make v2-conversation-smoke` started uvicorn hot-path plus the tomoko heartbeat process with fake runtime providers, sent float32 audio bytes through `/ws`, and observed `transcript -> durable_utterance -> model_delta -> model_complete -> audio_complete -> prompt_complete` with 1 binary WAV chunk. Real perceived first content/audio remains pending for the full dflash/VOICEVOX runtime. |
| 2026-06-18 | Tomoko v2 TTS echo suppression hotfix | server-owned WAV duration + 800ms grace before VAD/STT | real conversation latency not measured; smoke passed | Live tmux logs showed Tomoko TTS output looping back into LLM/VOICEVOX as repeated user turns. Added VAD reset plus hot-path mic suppression for each sent complete WAV chunk duration plus grace. Verification: focused audio unit 15 passed, `make check` 38 passed / 1 deselected with ruff pass, `make v2-conversation-smoke` fake `/ws` path passed, and `make v2-llm-tts-smoke` against live dflash/VOICEVOX returned `text="了解。"` with one 35372-byte audio chunk. Browser speaker/mic live recheck is still needed for perceived latency and echo guard tuning. |
| 2026-06-18 | Tomoko v2 blank STT loop correction | headset premise, no mic suppression during Tomoko speech, blank final STT ignored | real conversation latency not measured; smoke passed | The prior echo suppression hotfix is not appropriate under the headset premise because it prevents barge-in while Tomoko is speaking. Removed the hot-path mic suppression and instead fixed the headset-independent loop candidate: blank final STT is no longer adopted as a durable user utterance or prompt request. Verification: focused audio unit 14 passed, `make check` 37 passed / 1 deselected with ruff pass, `make v2-conversation-smoke` fake `/ws` path passed, and live uvicorn reload picked up the changed hot-path server. Next live check should watch for `blank_final_stt_ignored`; if repeated non-empty transcripts remain, log VAD RMS / segment duration / STT text. |
| 2026-06-18 | Tomoko v2 VOICEVOX 2x speech speed | `VoicevoxChunkedTtsBackend.speedScale=2.0` / `TOMOKO_V2_VOICEVOX_SPEED` | real audio latency not measured; unit contract passed | Changed root v2 VOICEVOX default speech speed to 2.0 and exposed `TOMOKO_V2_VOICEVOX_SPEED` for runtime override. Focused unit verifies `audio_query["speedScale"] == 2.0` and default real prompt executor uses 2.0. Real VOICEVOX runtime was not running in this shell, so perceived speed and first-audio impact still need `make tmux-runtime` plus live listening or `make v2-llm-tts-smoke`. |
| 2026-06-18 | Tomoko v2 real `/ws` say latency smoke | macOS `say` -> 16kHz mono float32 chunks -> `/ws` -> VAD/Apple Speech/dflash 26B/VOICEVOX 2.0x | voice-end to first binary audio 2875.8ms | Added `scripts/v2_say_latency_smoke.py` and `make v2-say-latency-smoke` to replay generated speech into the real hot-path WebSocket while the tmux runtime is running. Input: `トモコ、短く返事して。`, voice `Kyoko`, 128-sample realtime chunks, trailing silence 2500ms. STT recognized `智子短く返事して`; reply was `了解。短く話すね。`; one 52,780-byte WAV chunk was returned. Tool-side voice-end to transcript / TTS result / first binary audio were 2875.7 / 2875.8 / 2875.8ms. Server console showed `vad_segment -> stt_start -> apple_speech_done -> stt_done -> prompt_send` with the full prompt, then model/TTS/audio events. Artifact: `logs/say-latency-20260618-120425.json`; input WAV: `logs/say-latency-20260618-120425-input.wav`. |
| 2026-06-18 | Tomoko v2 VOICEVOX 1.5x speech speed default | `VoicevoxChunkedTtsBackend.speedScale=1.5` / `TOMOKO_V2_VOICEVOX_SPEED` | live audio latency not remeasured; unit contract passed | User listening feedback was that 2.0x was too fast, so root v2 now uses 1.5x as the default speech speed. `VoicevoxChunkedTtsBackend` and `create_default_real_prompt_executor()` both fall back to 1.5 when env is unset, and `Makefile` defaults `TOMOKO_V2_VOICEVOX_SPEED ?= 1.5`. Verification: `make check` passed with 44 unit tests / 1 deselected and ruff. The prior `say` latency smoke was measured under 2.0x, so first-audio latency should be remeasured after a live 1.5x run. |
| 2026-06-18 | Tomoko v2 scheduler fake vertical slice | `STT -> saturation -> scheduler -> LLM text -> speech-order -> TTS` | total 0.9ms | action `replace_current`, mode `replace_current`, audio chunks 1, artifact `logs/scheduler-conversation-smoke-20260618-131947.json`. |
| 2026-06-18 | Tomoko v2 scheduler real say latency smoke | `say -> /ws -> Apple Speech -> scheduler -> dflash -> speech-order -> VOICEVOX` | voice-end to first audio 2862.5ms | artifact `logs/scheduler-say-latency-20260618-132107.json`. |
| 2026-06-18 | Tomoko v2 DB split fake LISTEN/NOTIFY smoke | `hot-path STT -> DB/NOTIFY -> tomoko-process -> DB/NOTIFY -> hot-path TTS` | total 67.6ms | transcript->order 0.1ms, order->first audio 0.2ms, artifact `logs/db-split-smoke-20260618-133937.json`. |
| 2026-06-18 | Tomoko v2 DB split fake LISTEN/NOTIFY smoke | `hot-path STT -> DB/NOTIFY -> tomoko-process -> DB/NOTIFY -> hot-path TTS` | total 70.6ms | transcript->order 0.1ms, order->first audio 0.2ms, artifact `logs/db-split-smoke-20260618-134123.json`. |
| 2026-06-18 | Tomoko v2 DB split fake LISTEN/NOTIFY smoke | `hot-path STT -> DB/NOTIFY -> tomoko-process -> DB/NOTIFY -> hot-path TTS` | total 57.9ms | transcript->order 0.2ms, order->first audio 0.2ms, artifact `logs/db-split-smoke-20260618-140003.json`. |
| 2026-06-18 | Tomoko v2 DB split fake LISTEN/NOTIFY smoke | `hot-path STT -> DB/NOTIFY -> tomoko-process -> DB/NOTIFY -> hot-path TTS` | total 49.4ms | transcript->order 0.1ms, order->first audio 0.2ms, artifact `logs/db-split-smoke-20260618-140130.json`. |
| 2026-06-18 | Tomoko v2 DB split process-lifetime DB connections | `say -> /ws -> Apple Speech -> DB/NOTIFY -> tomoko-db -> dflash -> DB/NOTIFY -> hot-path -> VOICEVOX` | voice-end to first audio 2153.8ms | Removed per-utterance DB connection opens from hot-path and tomoko-db worker. Hot-path now warms its LISTEN/write connections before `/ws` ready; tomoko-db keeps LISTEN/work connections for process lifetime. Fake DB split smoke after warm connection: server total 15.8ms, notify->order 13.5ms, order->first audio 2.3ms, artifact `logs/db-split-smoke-20260618-140130.json`. Real split say smoke: STT `智子短く返事して`, reply `了解。短く話すね。`, server STT-start to audio-ready 1733.9ms, notify->order 826.3ms, order->VOICEVOX ready 607.4ms, tool voice-end to first binary audio 2153.8ms, artifact `logs/say-latency-20260618-140145.json`. Previous split real smoke before connection warm was 2982.3ms, so this run was 828.5ms faster. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 3491.2ms; p95 4387.7ms | turns=5, artifact `logs/five-turn-smoke-20260618-140934.json`. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 2717.7ms; p95 3642.5ms | turns=5, artifact `logs/five-turn-smoke-20260618-141915.json`. |
| 2026-06-18 | Tomoko v2 DB split fake LISTEN/NOTIFY smoke | `hot-path STT -> DB/NOTIFY -> tomoko-process -> DB/NOTIFY -> hot-path TTS` | total 52.6ms | transcript->order 0.1ms, order->first audio 0.2ms, artifact `logs/db-split-smoke-20260618-143511.json`. |
| 2026-06-18 | Tomoko v2 DB split fake LISTEN/NOTIFY smoke | `hot-path STT -> DB/NOTIFY -> tomoko-process -> DB/NOTIFY -> hot-path TTS` | total 57.5ms | transcript->order 0.1ms, order->first audio 0.2ms, artifact `logs/db-split-smoke-20260618-143545.json`. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 2931.7ms; p95 4691.3ms | turns=5, artifact `logs/five-turn-smoke-20260618-143614.json`. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 2575.3ms; p95 3707.6ms | turns=5, artifact `logs/five-turn-smoke-20260618-143729.json`. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 2902.8ms; p95 4111.2ms | turns=5, artifact `logs/five-turn-smoke-20260618-143934.json`. |
| 2026-06-18 | Tomoko v2 DB split fake LISTEN/NOTIFY smoke | `hot-path STT -> DB/NOTIFY -> tomoko-process -> DB/NOTIFY -> hot-path TTS` | total 58.7ms | transcript->order 0.1ms, order->first audio 0.2ms, artifact `logs/db-split-smoke-20260618-144044.json`. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 2984.7ms; p95 3634.9ms | turns=5, artifact `logs/five-turn-smoke-20260618-145017.json`. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 2792.6ms; p95 3391.7ms | turns=5, artifact `logs/five-turn-smoke-20260618-145232.json`. |
| 2026-06-18 | Tomoko v2 five-turn real runtime smoke | `5x say -> /ws -> Apple Speech -> dflash -> VOICEVOX` | avg first audio 2354.5ms; p95 3073.2ms | turns=5, artifact `logs/five-turn-smoke-20260618-145708.json`. |
| 2026-06-18 | Tomoko v2 semantic E2B early-start smoke | `say prefix windows -> Apple Speech -> Gemma E2B saturation` | no early OK | final STT available at 3933.6ms, artifact `logs/semantic-early-smoke-20260618-151302.json`. |
| 2026-06-18 | Tomoko v2 semantic E2B early-start smoke | `say prefix windows -> Apple Speech -> Gemma E2B saturation` | first OK at 3281.3ms from speech start; lead 352.7ms | final STT available at 3634.0ms, artifact `logs/semantic-early-smoke-20260618-151319.json`. |
| 2026-06-18 | Tomoko v2 Apple Speech pseudo streaming partial + E2B `/ws` smoke | `say -> /ws -> VAD hot path partial -> Apple Speech accumulated partial -> Gemma E2B saturation -> scheduler -> speech-order` | partial speech-order 5108.4ms before final transcript; voice-end to first audio 4492.5ms | v1-style pseudo streaming partial was restored in v2. Confirmation artifact `logs/say-latency-20260618-152817.json`: partial `その今日の予定を教えて` at 9168.0ms, scheduler `replace_current` with saturation score 0.52 (semantic saturation 0.8), final transcript at 14276.5ms. This proves E2B can start before final STT on the real `/ws` path, but the first audio still arrived 4492.5ms after voice end because partial STT/E2B/LLM/TTS are awaited inside the audio receive loop. |
| 2026-06-18 | Tomoko v2 async partial/final lanes + concise partial reply | `say -> /ws -> async partial lane -> E2B -> short partial LLM/TTS; final STT waits for partial lane` | clean first-audio best 860.5ms; final-confirm run 1515.6ms | Moved partial STT/E2B/LLM/TTS and final STT off the WebSocket receive loop, coalesced queued partial chunks, protected in-flight partial TTS from final replace, and used a concise prompt for partial replies. Best clean artifact `logs/say-latency-20260618-160201.json`: first audio 860.5ms after voice end, partial `その今日の予定を教えて`, 112684-byte WAV. Final-confirm clean artifact `logs/say-latency-20260618-160314.json`: first audio 1515.6ms, final transcript 5123.1ms after voice end. Remaining variance comes from Apple Speech pseudo partial timing and repeated partial/final duplicate speech before reconcile. |
| 2026-06-18 | Tomoko v2 partial/final reconcile `/ws` smoke | `say -> /ws -> async partial lane -> partial speech-order -> final reconcile suppress` | voice-end to first audio 1736.5ms; final transcript 2095.1ms; duplicate audio removed | Reconciled final STT after an active partial reply. Artifact `logs/say-latency-20260618-161305.json`: partial `その今日の予定を教えて` created one speech-order and one 112684-byte audio response; final `智子今日の予定を教えてそれだけで大丈夫です` produced scheduler suppress reason `final reconciled with active partial reply`. Event counts showed `speech_order=1`, `tts_result=1`, `binary_audio=1`, so the final did not append a second reply. |
| 2026-06-18 | Tomoko v2 recorded-file `/ws` smoke | `recorded m4a/wav -> ffmpeg 16kHz mono PCM WAV -> /ws -> Apple Speech -> scheduler -> TTS` | voice-end to first audio 5864.3ms | Added `scripts/v2_say_latency_smoke.py --input-wav` for user-recorded files. Clean hot-path artifact `logs/say-latency-20260618-161626.json` used `_reference/test.m4a`, input duration 3708.0ms, final transcript `こんにちは今の気分を教えてくださいませ`, one speech-order / one binary audio. In this sample, partial transcripts were emitted but semantic saturation stayed below threshold, so the reply started from final STT rather than early partial. |
| 2026-06-19 | Distilled saturation scorer hot predict | `make-model/benchmark_saturation_latency.py`, model loaded once, warmup 1000 / repeats 10000 | mean 0.0744ms / p50 0.0734ms / p95 0.0878ms | Command: `uv run python make-model/benchmark_saturation_latency.py --model make-model/artifacts/jdd-gemma26b-1000-saturation-model.json --repeats 10000 --warmup 1000 "今日の予定を教えて"`. Model load was 0.4254ms, last prediction was `SATURATION=0.1294`, max sample was 2.1347ms. This measures in-process hot predict only, not `uv` / Python startup. |
| 2026-06-20 | Tomoko v2 scheduler real say latency smoke | `say -> /ws -> Apple Speech -> scheduler -> dflash -> speech-order -> VOICEVOX` | voice-end to first audio 4999.1ms | artifact `logs/scheduler-say-latency-20260620-033825.json`. |
| 2026-06-20 | Tomoko v2 scheduler real say latency smoke | `say -> /ws -> Apple Speech -> scheduler -> dflash -> speech-order -> VOICEVOX` | voice-end to first audio 1599.3ms | artifact `logs/scheduler-say-latency-20260620-033846.json`. |
| 2026-06-20 | Tomoko v2 distilled semantic early-start smoke | `say prefix windows -> Apple Speech -> distilled saturation` | first OK at 1600.8ms from speech start; lead 1999.3ms | final STT available at 3600.1ms, artifact `logs/semantic-early-smoke-20260620-033913.json`. |
